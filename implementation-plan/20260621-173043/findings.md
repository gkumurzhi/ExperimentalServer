# Findings

| Finding ID | Source | Severity | Area | Evidence | Root cause | Proposed outcome | Confidence |
|---|---|---:|---|---|---|---|---:|
| F-001 | `project-analysis-report.md`, `security-auditor.md`, `qa-expert.md` | HIGH | `SMUGGLE` runtime | Generated HTML artifacts stay on `Preparing file...` in a real browser because inline scripts do not run under the served `script-src 'self'` policy, and the encrypted path is not positively verified | Generated artifact execution model and CSP policy are misaligned, while smoke only covers the unchecked/default path | Make the artifact path execute correctly without loosening the global UI CSP, and add browser/server verification for plain and encrypted flows | High |
| F-002 | `architect-reviewer.md`, `qa-expert.md` | MEDIUM | Renderer contract | `generate_smuggling_html()` mixes payload prep and inline template rendering, accepts `crypto_js_src`, and the encrypted template ignores that option | Public renderer behavior is under-specified and under-tested | Introduce a small renderer seam and direct contract tests that pin plaintext/encrypted output and script-path behavior | High |
| F-003 | `documentation-engineer.md`, `security-auditor.md`, `cli-developer.md` | HIGH | Operator framing | README uses bypass/delivery language, CLI help offers `Public HTTPS` with `--tls`, and top-level docs overstate lab-only capabilities | Operator-facing copy is not consistently aligned with the safer profile model and public-direct validation rules | Rewrite framing as lab-only/controlled, update CLI help, and add stale-doc guardrails for unsafe phrases | High |
| F-004 | `frontend-developer.md`, `accessibility-tester.md` | MEDIUM | Artifact UI | The `HTML` action and dialog auto-open the generated artifact before it can be inspected, copied, or saved, and initial focus lands on the side-effect action | Presentation favors immediate popup behavior over explicit artifact handling | Switch to a generated-result state with explicit `Copy URL` / `Open` / `Save` actions and safer initial focus | High |
| F-005 | `security-auditor.md` | MEDIUM | Plugin policy | Plugins can register disabled built-in method names, such as `SMUGGLE` in `workspace`, unless override intent is made explicit | Core-method reservation checks only currently registered handlers, not the global built-in namespace | Reserve built-in method names across profiles unless an explicit override mode is enabled, and add regression tests | High |
| F-006 | `qa-expert.md`, `cli-developer.md` | MEDIUM | Config clarity | `smuggle_temp_*` knobs are not covered through settings precedence tests, and `--print-config` can make effective TLS look disabled in `sslip` or ACME flows | Config/test coverage is narrower than the operator-facing settings surface | Add precedence coverage for temp-artifact knobs and clarify runtime TLS/public-direct behavior in samples or output docs | Medium |
| F-007 | `frontend-developer.md` | LOW | Request panel density | Secondary batch controls and disabled methods share first-screen prominence with the primary request flow | Presentation does not distinguish first-run tasks from advanced or unavailable controls | Keep this as backlog unless STAGE-004 leaves the first-run UX materially confusing | Medium |
