# STAGE-004 Closure Report
_Generated: 2026-06-21 18:09:14 MSK_

## Stage target
- Stage: STAGE-004 - Explicit artifact UI and keyboard safety
- Active plan: `implementation-plan/20260621-173043`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was empty after STAGE-003)
- Dependencies: satisfied (`Depends on: STAGE-002`)

## Execution approach
1. Convert the current `SMUGGLE` browser smoke expectations from auto-open behavior to an explicit generated-artifact result flow and watch them fail.
2. Update the artifact UI around the existing managed-dialog system so generation and follow-up actions are separate, keyboard-safe, and reuse current clipboard/download helpers.
3. Rerun `lab/full` plus disabled-state smoke, then update the stage artifacts if the new keyboard contract holds.

## Implemented changes
1. Renamed the file-row `SMUGGLE` action and dialog copy so the operator now generates an HTML artifact instead of triggering an implicit open side effect.
2. Split success handling into an explicit result dialog that shows encryption state, absolute artifact URL, optional password, and separate `Copy URL`, `Open`, `Save`, and `Close` actions.
3. Moved initial dialog focus to the XOR checkbox, restored focus back to the originating row action, and expanded browser smoke coverage to lock in the new keyboard order plus unchanged disabled-profile behavior.

## Acceptance criteria from plan
- [x] The artifact flow no longer auto-opens on success
- [x] Initial dialog focus no longer lands on the side-effect action
- [x] Browser smoke proves the generated-result state and keyboard path in `lab`, while `workspace` and `serve` stay unavailable

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted browser smoke | `python tools/browser_smoke.py --profile lab --mode full` | PASS | Artifact flow now stays in-page until explicit `Open`, and smoke verified the result-dialog tab order, clipboard path, and focus restoration. |
| Disabled-profile smoke | `python tools/browser_smoke.py --profile workspace --mode disabled-state` | PASS | `workspace` still reports `"smuggle": false`; no artifact entry point surfaced. |
| Disabled-profile smoke | `python tools/browser_smoke.py --profile serve --mode disabled-state` | PASS | `serve` still reports `"smuggle": false`; no artifact entry point surfaced. |
| Diff hygiene | `git diff --check -- src/data/static/ui/core.js src/data/static/ui/files.js src/data/static/ui/components.css tools/browser_smoke.playwright.js implementation-plan/20260621-173043/stage-reports/STAGE-004-20260621-180914.md implementation-plan/20260621-173043/stage-status.md implementation-plan/20260621-173043/stages/STAGE-004-explicit-artifact-ui-and-keyboard-safety.md implementation-plan/20260621-173043/change-log.md` | PASS | No whitespace or conflict-marker issues in the staged scope. |
| Manual/static review | `git diff -- src/data/static/ui/core.js src/data/static/ui/files.js src/data/static/ui/components.css tools/browser_smoke.playwright.js` | PASS | Reviewed the UI diff to confirm explicit follow-up actions, updated translation labels, and result-dialog styling stayed scoped to the artifact flow. |
