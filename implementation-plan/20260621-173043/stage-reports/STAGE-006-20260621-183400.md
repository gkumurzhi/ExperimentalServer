# STAGE-006 Closure Report
_Generated: 2026-06-21 18:34:00 MSK_

## Stage target
- Stage: STAGE-006 - SMUGGLE temp-control config clarity
- Active plan: `implementation-plan/20260621-173043`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was empty after STAGE-005)
- Dependencies: satisfied (`Depends on: STAGE-003`)

## Execution approach
1. Add regression coverage for `smuggle_temp_*` precedence and for the confusing `--print-config` TLS view.
2. Verify the new tests fail for the expected missing-field reason before changing runtime output.
3. Implement the smallest additive clarification in settings/sample config output, then rerun the full stage verification set.

## Implemented changes
1. Added settings precedence coverage that proves `smuggle_temp_age`, `smuggle_temp_file_limit`, and `smuggle_temp_storage_limit_mb` resolve correctly through INI, environment, and CLI layers and still convert to runtime kwargs correctly.
2. Added `effective_tls_enabled()` plus an additive `effective_tls` field in `ServerSettings.to_redacted_dict()`, so `--print-config` shows runtime HTTPS activation even when the raw self-signed `tls` flag remains `false`.
3. Updated the sample public-direct config text and both deployment example INI files to spell out that public exposure stays on `workspace`/`serve` and that `sslip`/ACME/cert+key can enable runtime TLS without `tls = true`.

## Acceptance criteria from plan
- [x] Settings precedence tests cover the `smuggle_temp_*` knobs through INI, environment, and CLI resolution
- [x] Operators have a clear explanation for why validated `sslip` or ACME configs may still show `"tls": false` in normalized output, or an additive output field makes the runtime TLS state explicit
- [x] Deployment samples continue to validate and keep `lab` out of the public-direct path

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Red/green regression pass | `./.venv/bin/pytest -q tests/test_settings.py::test_smuggle_temp_limits_follow_file_env_and_cli_precedence tests/test_settings.py::test_redacted_json_reports_effective_tls_when_enabled_via_sslip tests/test_cli.py::TestCLIMain::test_main_print_config_reports_effective_tls_for_sslip` | PASS | Final green state after the new `effective_tls` output and precedence coverage landed; the same CLI/settings regressions failed first when `effective_tls` was absent. |
| Targeted tests | `./.venv/bin/pytest -q tests/test_settings.py tests/test_cli.py tests/test_deployment_artifacts.py` | PASS | `144 passed in 4.36s`; settings resolution, CLI wiring, and deployment artifacts all stayed green. |
| Default config validation | `python -m exphttp --check-config` | PASS | Default normalized config validates without startup. |
| Docker sample validation | `python -m exphttp --config deploy/docker/exphttp.ini.example --check-config` | PASS | Docker public-direct sample still validates after the comment updates. |
| systemd sample validation | `python -m exphttp --config deploy/systemd/exphttp.ini.example --check-config` | PASS | systemd public-direct sample still validates after the comment updates. |
| Manual/static review | `python -m exphttp --sslip --print-config` plus `rg -n "effective_tls|public_direct on workspace/serve|Runtime TLS becomes active" src/settings.py deploy/docker/exphttp.ini.example deploy/systemd/exphttp.ini.example` | PASS | Printed config now shows `"tls": false` alongside `"effective_tls": true`, and the sample comments explicitly keep `lab` out of the public-direct path. |
| Diff hygiene | `git diff --check -- src/settings.py tests/test_settings.py tests/test_cli.py deploy/docker/exphttp.ini.example deploy/systemd/exphttp.ini.example implementation-plan/20260621-173043/stage-reports/STAGE-006-20260621-183400.md implementation-plan/20260621-173043/stage-status.md implementation-plan/20260621-173043/stages/STAGE-006-smuggle-temp-control-config-clarity.md implementation-plan/20260621-173043/change-log.md` | PASS | No whitespace or conflict-marker issues in the stage scope. |
