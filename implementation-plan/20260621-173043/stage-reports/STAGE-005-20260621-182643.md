# STAGE-005 Closure Report
_Generated: 2026-06-21 18:26:43 MSK_

## Stage target
- Stage: STAGE-005 - Plugin core-method reservation
- Active plan: `implementation-plan/20260621-173043`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was empty after STAGE-004)
- Dependencies: satisfied (`Depends on: None`)

## Execution approach
1. Re-read the plugin registration path and the existing regression tests around core-method collisions.
2. Prove whether disabled built-in names are already reserved across profiles before making any runtime edits.
3. Tighten the operator docs only if the runtime already matches the intended boundary, then rerun the focused verification set.

## Implemented changes
1. Verified that `ExperimentalHTTPServer._register_plugins()` already computes the union of built-in registry methods across all profiles and rejects plugin reuse of those names unless `plugins_override_core` is enabled.
2. Left the runtime unchanged because the current implementation and tests already satisfy the reservation requirement without widening plugin behavior.
3. Clarified README and architecture docs so profile capability gating is explicitly separate from core-method override intent, including the `workspace`/`serve` + `SMUGGLE`/`NOTE` case.

## Acceptance criteria from plan
- [x] A plugin cannot register `SMUGGLE` in `workspace` or `serve` unless the explicit core-override path is enabled
- [x] Existing legitimate plugin registration paths still work
- [x] Docs describe the difference between capability gating and core-method override intent

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted tests | `./.venv/bin/pytest -q tests/test_extensions.py tests/test_server_methods.py` | PASS | `113 passed in 3.05s`; plugin registration keeps valid methods working and rejects reserved core names by default. |
| Registry regression guard | `./.venv/bin/pytest -q tests/test_handler_registry.py` | PASS | `13 passed in 0.52s`; handler registration paths remain healthy with the current reservation logic. |
| Manual/static review | `sed -n '300,370p' src/server.py` plus README/architecture diff review | PASS | `_register_plugins()` reserves the built-in method union across profiles, and the docs now say disabled core names stay reserved without explicit override. |
| Diff hygiene | `git diff --check -- README.md docs/architecture.md implementation-plan/20260621-173043/stage-reports/STAGE-005-20260621-182643.md implementation-plan/20260621-173043/stage-status.md implementation-plan/20260621-173043/stages/STAGE-005-plugin-core-method-reservation.md implementation-plan/20260621-173043/change-log.md` | PASS | No whitespace or conflict-marker issues in the stage scope. |
