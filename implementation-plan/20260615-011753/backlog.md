# Backlog and Needs-Investigation Items

| Item | Source | Why not staged now | Suggested next step |
|---|---|---|---|
| Durable Secure Notepad recovery | `security-auditor.md` F4; `product-manager.md` Open Questions | Needs crypto/product ADR before implementation; persisting current session keys is explicitly unsafe. | Write a separate ADR covering envelope encryption, key wrapping, metadata privacy, nonces, recovery, and test vectors after Notepad is chosen as a product track. |
| App-side trusted-proxy identity model | `security-auditor.md` F2 | Current safe stance is direct-peer IP plus proxy-side rate limiting; trusting forwarded headers without CIDR/model would be risky. | If hosted reverse-proxy support becomes official, design `--trusted-proxy-cidr` and regression tests. |
| Public API v1 implementation | `api-designer.md` Deeper Improvements | STAGE-010 only documents v0 and v1 scope; implementing `/api/v1` needs product commitment and client demand. | After STAGE-010, create a dedicated v1 plan with capability, pagination, idempotency, and error-envelope conformance tests. |
| SDK/client work | `api-designer.md` Open Questions | Premature before v0/v1 boundary and stable errors. | Wait until API v1 design is accepted. |
| PyPI/GHCR publishing lane | `devops-engineer.md`; `docker-expert.md` | Publishing requires maintainer SLA, credentials/OIDC boundaries, promotion policy, scan gates, provenance, and rollback ownership. | Create a distribution plan only after the project chooses a supported public artifact surface. |
| Image vulnerability scan, image SBOM/provenance, digest rollback | `docker-expert.md`; `dependency-manager.md` | Only needed if Docker becomes a supported registry artifact. | Add after Docker support status changes from example/operator convenience to published artifact. |
| Streaming uploads and cached quota accounting | `performance-engineer.md` Deeper Improvements | Requires workload targets and benchmark evidence; STAGE-009 only captures baselines and the no-limit scan fix. | Use STAGE-009 results to decide whether streaming/cached accounting is worth the complexity. |
| Cursor-style large directory listing | `performance-engineer.md` Open Questions | Depends on whether exact `INFO.total_items` remains a product/API promise. | Decide listing semantics after INFO benchmarks and API stability work. |
| Full WebSocket v1 collaboration/resume protocol | `websocket-engineer.md` Deeper Improvements | Legacy `/notes/ws` is acceptable for bundled lab client; collaboration requires revisions, replay, liveness, and idempotency design. | Create a separate WebSocket v1 stage only if Notepad becomes workspace-grade. |
| Frontend build pipeline | `frontend-developer.md` Deeper Improvements | Not needed for safe default migration; static no-build UI remains sufficient. | Reconsider only if UI product scope grows beyond static capability-driven controls. |
| Local ignored `uv.lock` drift policy | `dependency-manager.md` P2 | Lower priority than Python 3.14 readiness and not a direct CI blocker. | Document local lock non-authority or add a lightweight check only if drift keeps confusing reviews. |
