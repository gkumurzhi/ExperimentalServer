# Findings

| Finding ID | Source | Severity | Area | Evidence | Root cause | Proposed outcome | Confidence |
|---|---|---:|---|---|---|---|---:|
| F-001 | `project-analysis-report.md` Key Medium Issues; `documentation-engineer.md` P0; `qa-expert.md` P1 | MEDIUM | Docs/CI | `python tools/sync_docs.py --check` fails for `API.md -> docs/api.md` and `CONTRIBUTING.md -> docs/contributing.md`. | Generated docs mirrors drifted from root-canonical docs. | Regenerate mirrors and add local/PR guardrails so docs drift is caught before CI. | High |
| F-002 | `documentation-engineer.md` P1/P2; `product-manager.md` Quick Wins | MEDIUM | Release docs | Closed remediation work remains under `[Unreleased]`; docs mention install/public artifact paths while release workflow is artifact-only. | Release boundary and distribution stance are not reflected consistently across docs. | Add a dated changelog/release boundary and clarify artifact-only publishing status. | High |
| F-003 | `product-manager.md` P0; `security-auditor.md` F1; `docker-expert.md` Issues | MEDIUM | Product/security | `DEFAULT_PROFILE = "lab"` and CLI/Docker defaults expose experimental/destructive features unless operators choose safer flags. | New-user default is still lab-first while product direction points to safe file workspace. | Record the default-profile decision and migrate toward `workspace` only after compatibility docs and tests are ready. | High |
| F-004 | `architect-reviewer.md` Issues; `project-analysis-report.md` Architecture & Design | MEDIUM | Architecture | Profile/capability policy is interpreted across `src/features.py`, `src/server.py`, CORS, handlers, UI, docs, and tests. | Profile-derived behavior is scattered instead of flowing through one reusable policy boundary. | Centralize profile-derived method, CORS, browser mutation, WebSocket, and destructive-operation policy. | High |
| F-005 | `security-auditor.md` F2; `product-manager.md` P1; `documentation-engineer.md` Open Questions | MEDIUM | Security docs | Basic Auth throttling keys on direct socket peer IP; reverse-proxy deployments are policy-only and under-specified. | No trusted-proxy identity model exists, but docs mention reverse-proxy deployments. | Document direct-peer semantics and proxy-side per-client throttling requirements; defer app trusted-proxy support. | High |
| F-006 | `qa-expert.md` P2/P3; `project-manager.md` STAGE-006 | MEDIUM | QA | CI uses global coverage and broad tests; no named high-risk lanes for parser/auth/CORS/profile/storage/WS/Notepad. | Existing coverage is broad but not risk-specific enough for default-profile changes. | Add named deterministic risk lanes and record no-regression baselines. | Medium |
| F-007 | `qa-expert.md` P2; `frontend-developer.md` Issue 4; `accessibility-tester.md` Coverage | MEDIUM | Frontend/QA | Browser smoke hardcodes `profile="lab"` and does not cover `serve`/`workspace` disabled-state UX. | Release smoke protects the lab happy path, not safe-profile UI behavior. | Add profile-aware browser smoke with minimal `serve` and `workspace` disabled-state assertions. | High |
| F-008 | `frontend-developer.md` Issues; `accessibility-tester.md` P1/P2 | MEDIUM | UI/a11y | Notepad UI does not independently honor `note_delete`/`note_clear`; recovery warning text exists but is not rendered; textarea lacks explicit label. | UI uses coarse Notepad availability and under-surfaces session/recovery risk. | Gate destructive Notepad controls by capability, render warning, improve labels and error announcements. | High |
| F-009 | `devops-engineer.md` Issue 1; `docker-expert.md` Issues | MEDIUM | DevOps/Docker | Release artifacts expire after 30 days while rollback docs refer to previous verified artifacts; Docker examples/defaults need explicit safe profile status. | Artifact-of-record and Docker support boundaries are not explicit enough. | Decide durable release assets vs retention and make Docker examples/profile stance explicit. | High |
| F-010 | `dependency-manager.md` P1; `project-analysis-report.md` DevOps & Infrastructure | MEDIUM | Dependencies | `requires-python` caps at `<3.14`; CI only covers 3.10-3.13 though Python 3.14 readiness needs a decision. | Python support policy is capped by CI policy, not known failures. | Add Python 3.14 constrained CI first, then widen metadata/docs only if clean. | High |
| F-011 | `api-designer.md` Summary/Issues; `documentation-engineer.md` P2 | MEDIUM | API | Current custom methods and mixed error shapes are de facto public but lack an explicit legacy v0/v1 boundary. | Lab-friendly API grew without a versioned public-client contract. | Document current API as legacy v0 and defer opt-in v1 design until client work is chosen. | High |
| F-012 | `performance-engineer.md` Issues | MEDIUM | Performance | Upload quota scans run even when aggregate limits are disabled; `INFO` listings materialize/sort all entries before slicing; no benchmark baselines. | Hot paths are guarded for correctness but lack performance evidence and one no-limit short-circuit. | Add benchmarks, record baselines, and fix no-limit quota scan behavior. | High |
| F-013 | `websocket-engineer.md` Issues; `security-auditor.md` F3 | MEDIUM | WebSocket/Notepad | WS delete/clear lack message-level capability checks; `opId` is only correlation; send failures are swallowed; binary frame policy is unclear. | Legacy lab transport does not yet have workspace-grade operation semantics. | Add bounded safety fixes and documentation without promoting `/notes/ws` to a public sync protocol. | High |
| F-014 | `security-auditor.md` F4; `product-manager.md` Open Questions; `project-manager.md` Deeper Improvements | MEDIUM | Product/security | Durable Notepad recovery, public publishing, trusted proxy, and API-client strategy need decisions before implementation. | Strategic tracks have high support/security implications and insufficient product commitment. | Keep these items in backlog unless a maintainer explicitly chooses the track. | Medium |
