# STAGE-006 Closure Report
_Generated: 2026-06-15 14:40:27 +0300_

## Stage target
- Stage: STAGE-006 — Profile-aware smoke and risk test gates
- Requested by: $close-plan-stage STAGE-006 --from implementation-plan/20260615-011753/stages/STAGE-006-profile-aware-smoke-risk-test-gates.md --with-subagents
- Active plan: `implementation-plan/20260615-011753`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was empty)
- Dependencies: satisfied (`STAGE-004` and `STAGE-005` are CLOSED)

## Execution approach
1. Add profile/mode switches to `tools/browser_smoke.py` while keeping full `lab` smoke as the default behavior.
2. Reuse existing Playwright smoke helpers to add minimal `serve` and `workspace` disabled-state assertions.
3. Add named CI risk-lane steps without replacing the full pytest/coverage suite.
4. Run targeted help/static/risk-lane checks and smoke modes where the local browser environment permits.

## Acceptance criteria from plan
- [x] Browser smoke can run full lab behavior and minimal `serve`/`workspace` disabled-state behavior.
- [x] CI or contributor docs expose named risk lanes for parser/framing, auth, CORS/profile, storage/quota, and WebSocket/Notepad.
- [x] Profile-disabled UI expectations are asserted for the chosen default path.
- [x] Any new coverage threshold or no-regression rule has a recorded baseline.

## Subagents
- `explorer` (`019ecb18-d3b2-7932-8a11-950a6b6cb60c`) inspected the scoped smoke, CI, and risk-test paths read-only. It recommended lab/full plus serve/workspace disabled-state smoke commands, named risk lanes without pytest markers, and confirmed the profile-disabled path should assert PING capabilities plus actual UI disabled states.

## Files changed
- `.github/workflows/ci.yml` - added named `risk-lanes` CI job and made release browser smoke run lab/full plus workspace/serve disabled-state profiles.
- `tools/browser_smoke.py` - added `--profile` and `--mode` options, kept `lab` full smoke as the default, and passed profile/mode context into Playwright.
- `tools/browser_smoke.playwright.js` - added `serve`/`workspace` profile capability assertions for request controls, ordinary upload controls, advanced upload disabled state, and Notepad unavailable/disabled UI.
- `implementation-plan/20260615-011753/stages/STAGE-006-profile-aware-smoke-risk-test-gates.md` - marked stage closed and recorded completion notes.
- `implementation-plan/20260615-011753/stage-status.md` - updated STAGE-006 row to CLOSED.
- `implementation-plan/20260615-011753/change-log.md` - added STAGE-006 closure entry.
- `implementation-plan/20260615-011753/stage-reports/STAGE-006-20260615-144027.md` - recorded this closure report.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Browser smoke help | `python tools/browser_smoke.py --help` | PASS | Help shows `--profile {lab,workspace,serve}` and `--mode {auto,full,disabled-state}`. |
| JavaScript syntax | `node --check tools/browser_smoke.playwright.js` | PASS | Playwright smoke script parses. |
| Static UI check | `python tools/check_static_ui_assets.py --repo-root .` | PASS | Static UI asset checks passed. |
| Workspace profile smoke | `python tools/browser_smoke.py --profile workspace --mode disabled-state` | PASS | Asserted workspace capabilities and disabled advanced-upload/Notepad state. |
| Serve profile smoke | `python tools/browser_smoke.py --profile serve --mode disabled-state` | PASS | Asserted serve capabilities and disabled upload/advanced-upload/Notepad state. |
| Parser/framing risk lane | `python -m pytest -q tests/test_property/test_request_parser.py tests/test_property/test_ws_frame_parser.py tests/test_http/test_content_length_smuggling.py tests/test_security/test_websocket_frame_limit.py` | FAIL | Initial local run failed during collection because `hypothesis` was missing from the system Python. |
| Auth risk lane | `python -m pytest -q tests/test_security/test_auth.py` | PASS | `34 passed`. |
| Ruff check | `ruff check tools/browser_smoke.py` | PASS | Python wrapper lint passed. |
| Ruff format check | `ruff format --check tools/browser_smoke.py` | FAIL | Wrapper needed formatting after edits. |
| Ruff format | `ruff format tools/browser_smoke.py` | PASS | Reformatted wrapper. |
| Test dependency install attempt | `PIP_CONSTRAINT=constraints/ci.txt python -m pip install -e ".[dev,test]"` | FAIL | System Python is externally managed by PEP 668; did not install into it. |
| Temporary Hypothesis install | `rm -rf /tmp/exphttp-stage006-python-deps && PIP_CONSTRAINT=constraints/ci.txt python -m pip install --target /tmp/exphttp-stage006-python-deps hypothesis` | PASS | Installed Hypothesis only into `/tmp` for local property-lane verification. |
| Ruff check and format check | `ruff check tools/browser_smoke.py && ruff format --check tools/browser_smoke.py` | PASS | `All checks passed`; wrapper already formatted. |
| Parser/framing risk lane rerun | `PYTHONPATH=/tmp/exphttp-stage006-python-deps python -m pytest -q tests/test_property/test_request_parser.py tests/test_property/test_ws_frame_parser.py tests/test_http/test_content_length_smuggling.py tests/test_security/test_websocket_frame_limit.py` | PASS | `17 passed`. |
| CORS/profile risk lane | `PYTHONPATH=/tmp/exphttp-stage006-python-deps python -m pytest -q tests/test_handler_registry.py tests/test_request_pipeline.py tests/test_server_methods.py tests/test_security/test_websocket_upgrade.py` | PASS | `138 passed`. |
| Storage/quota risk lane | `PYTHONPATH=/tmp/exphttp-stage006-python-deps python -m pytest -q tests/test_handlers/test_files.py tests/test_handlers/test_notepad.py` | PASS | `76 passed`. |
| WebSocket/Notepad risk lane | `PYTHONPATH=/tmp/exphttp-stage006-python-deps python -m pytest -q tests/test_websocket.py tests/test_websocket_handlers.py tests/test_handlers/test_notepad.py tests/test_security/test_websocket_upgrade.py` | PASS | `145 passed`. |
| Lab full browser smoke | `python tools/browser_smoke.py --profile lab --mode full` | PASS | Existing full lab path and notepad-unavailable path passed. |

## CI workflow review
- `.github/workflows/ci.yml` keeps the existing Python matrix and full `pytest --cov=src --cov-report=term-missing --cov-report=xml --cov-fail-under=65` coverage gate.
- New `risk-lanes` job installs `.[dev,test]` on Python 3.12 and exposes deterministic named lanes for parser/framing, auth, CORS/profile policy, storage/quota, and WebSocket/Notepad.
- Release smoke now runs `tools/browser_smoke.py` explicitly for `lab` full mode plus `workspace` and `serve` disabled-state modes.
- No new stricter coverage threshold or no-regression rule was added, so no new coverage baseline is required.

## Outcome
Status: CLOSED

All acceptance criteria are met. Remaining local setup note: the system Python lacks Hypothesis and is PEP 668 externally managed, so property-lane verification used a temporary `/tmp` dependency target; CI installs the declared test extras directly.
