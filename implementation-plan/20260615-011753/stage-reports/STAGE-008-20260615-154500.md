# STAGE-008 Closure Report
_Generated: 2026-06-15 15:45:00 +0300_

## Stage target
- Stage: STAGE-008 - Docker and rollback boundary
- Requested by: `$close-plan-stage STAGE-008 --from implementation-plan/20260615-011753/stages/STAGE-008-docker-rollback-boundary.md --with-subagents`
- Active plan: `implementation-plan/20260615-011753`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; STAGE-002 is CLOSED in `stage-status.md`

## Execution approach
1. Confirm the STAGE-002 Docker/profile stance and current Docker/release/docs behavior.
2. Use a narrowly scoped subagent to inspect Docker, release, and rollback boundaries for this stage.
3. Make Docker Compose profiles explicit and keep host publication loopback-local unless the ACME profile intentionally requires public ports.
4. Align release artifact retention and rollback documentation to a concrete availability window.
5. Run Compose config, docs mirror/stale checks, static workflow/doc review, and final diff checks.
6. Update the selected stage, stage status, change log, and this closure report.

## Acceptance criteria from plan
- [x] Docker examples pass an explicit safe profile or clearly document intentional `lab` usage and required auth/TLS/firewall controls.
- [x] Release rollback docs point to artifacts that remain available for the documented rollback window.
- [x] The project states whether Docker is example/operator convenience or a supported registry artifact.
- [x] Docker smoke or docs are updated if Compose/example profile behavior changes.

## Subagent review
- Agent: `explorer` (`019ecb53-1aa7-7591-a241-691ad69e7734`)
- Scope: read-only inspection of Dockerfile, Compose, release/CI workflows, README, CONTRIBUTING, SECURITY, docs mirrors, ADR-006, and active STAGE-008 plan artifacts.
- Result: confirmed the closeability gaps: Docker app profile was implicit, release rollback relied on 30-day artifacts while docs implied previous verified artifacts would be available, README still deferred Docker rollback boundaries to this stage, CI Docker smoke did not assert `profile=workspace`, and Compose `stop_grace_period: 20s` needed operator wording against longer body/stream deadlines. The implemented changes address these gaps.

## Implementation log
- Updated `Dockerfile` to copy the public `exphttp/` package into the build stage so the container console script works, and made the image default command pass `--profile workspace` explicitly.
- Updated `examples/docker/docker-compose.yml` to distinguish Compose service profiles from the app capability profile, pass `--profile workspace` explicitly for default/auth-tls/acme services, require the Basic Auth secret for the ACME HTTPS service, and keep default host publication loopback-local.
- Updated `.github/workflows/release.yml` to retain release artifacts of record for 90 days, matching the documented rollback window and the `actions/upload-artifact` documented maximum checked via Context7.
- Updated `.github/workflows/ci.yml` so Docker smoke passes `--profile workspace` for TLS/auth and parses `PING` JSON to assert both `status=pong` and `profile=workspace`.
- Updated `README.md`, `CONTRIBUTING.md`, and `SECURITY.md` with Docker operator-convenience status, explicit app profile guidance, lab/exposure controls, ACME Basic Auth expectations, 20s container shutdown budget, 90-day Python artifact rollback, and operator-owned container rollback boundaries.
- Regenerated `docs/contributing.md` and `docs/security.md` from the root canonical files.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Documentation lookup | Context7 `/actions/upload-artifact` query for `retention-days` | PASS | Confirmed artifact retention input supports a 1-90 day range, so `90` is valid. |
| Compose config | `docker compose -f examples/docker/docker-compose.yml config` | PASS | Default service renders with explicit `--profile workspace` and loopback port publication. |
| Compose auth/TLS config | `docker compose -f examples/docker/docker-compose.yml --profile auth-tls config >/tmp/exphttp-compose-auth-tls.yml` | PASS | Optional auth/TLS service renders. |
| Compose ACME config | `docker compose -f examples/docker/docker-compose.yml --profile acme config >/tmp/exphttp-compose-acme.yml` | PASS | Optional ACME service renders with the auth secret mounted. |
| Docs mirror check | `python tools/sync_docs.py --check` | PASS | Documentation mirrors are in sync. |
| Stale-doc check | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
| Workflow/Compose YAML parse | Python `yaml.safe_load` for `.github/workflows/ci.yml`, `.github/workflows/release.yml`, and `examples/docker/docker-compose.yml` | PASS | Edited YAML parses. |
| CI Docker smoke shell syntax | Extracted `Docker smoke` step from `.github/workflows/ci.yml` and piped it to `bash -n` | PASS | Edited shell parses. |
| Whitespace/static check | `git diff --check` | PASS | No whitespace errors. |
| Optional Docker smoke, first run | `docker build -t exphttp:stage-008 .` then `docker run --rm exphttp:stage-008 --version` | FAIL | Exposed a Dockerfile bug: the build context copied `src/` but not `exphttp/`, so the console script could not import `exphttp.cli`. Fixed before closure. |
| Optional Docker smoke, final run | `docker build -t exphttp:stage-008 .`; `docker run --rm exphttp:stage-008 --version`; run container and `PING` loopback endpoint | PASS | `exphttp 2.0.0`; `PING profile=workspace`. |

## Files changed
- `.github/workflows/ci.yml`
- `.github/workflows/release.yml`
- `Dockerfile`
- `examples/docker/docker-compose.yml`
- `README.md`
- `CONTRIBUTING.md`
- `SECURITY.md`
- `docs/contributing.md`
- `docs/security.md`
- `implementation-plan/20260615-011753/stages/STAGE-008-docker-rollback-boundary.md`
- `implementation-plan/20260615-011753/stage-status.md`
- `implementation-plan/20260615-011753/change-log.md`
- `implementation-plan/20260615-011753/stage-reports/STAGE-008-20260615-154500.md`

## Final status
CLOSED
