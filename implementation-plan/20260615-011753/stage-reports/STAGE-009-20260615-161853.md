# STAGE-009 Closure Report
_Generated: 2026-06-15 16:18:53 +0300_

## Stage target
- Stage: STAGE-009 - Workspace performance baseline
- Requested by: `$close-plan-stage STAGE-009 --from implementation-plan/20260615-011753/stages/STAGE-009-workspace-performance-baseline.md --with-subagents`
- Active plan: `implementation-plan/20260615-011753`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was clean)
- Dependencies: satisfied (`STAGE-003` is `CLOSED`)
- Documentation check: Context7 used for `pytest-benchmark` fixture and `--benchmark-only` behavior (`/websites/pytest-benchmark_readthedocs_io_en_stable`)

## Execution approach
1. Inspect storage, INFO, Notepad, WebSocket slot, and existing test helpers for benchmarkable hot paths.
2. Add lightweight benchmark tests under `tests/benchmarks/` that expose current behavior without API semantic changes.
3. Optimize no-limit upload quota acceptance so aggregate usage scans are skipped only when both total file count and total storage limits are disabled.
4. Add focused regression tests for no-limit and limited quota behavior.
5. Run targeted tests and benchmark collection, then record baseline numbers and environment notes.
6. Update plan artifacts with closure status and report link.

## Acceptance criteria from plan
- [x] Benchmark files exist and can be collected by pytest.
- [x] Baseline benchmark results are recorded in the stage report with environment notes.
- [x] No-limit upload policy avoids unnecessary aggregate usage scans.
- [x] Existing quota behavior remains unchanged when upload limits are enabled.
- [x] `INFO` and Notepad benchmarks expose current behavior without changing API semantics.

## Subagent summary
- `explorer` mapped the relevant storage, INFO, Notepad, receive-loop, metric, and WebSocket slot paths.
- It independently confirmed that `_check_accepts()` should skip `current_usage()` for plain no-limit `UploadStoragePolicy()` while preserving aggregate checks when byte/file limits are enabled.
- It recommended bounded filesystem benchmarks and warned against hard thresholds or live-socket WebSocket benchmarks. The implementation used direct WebSocket slot saturation to avoid live network noise.

## Implementation summary
- `src/storage.py`: `_check_accepts()` now calls `current_usage()` only when aggregate byte or file-count limits are configured. Negative size validation and reserved-free-space checks still run.
- `tests/test_handlers/test_files.py`: added regression coverage proving no-limit uploads do not call `current_usage()` and aggregate-limited uploads still do.
- `tests/benchmarks/test_workspace_hot_paths.py`: added seven lightweight pytest-benchmark baselines for upload quota scan, no-limit upload publish, INFO listing, Notepad save/list, chunked request receive, and WebSocket slot saturation.
- Benchmark tests are collected and skipped cleanly when the optional `pytest-benchmark` package is absent.

## Files changed
- `src/storage.py`
- `tests/test_handlers/test_files.py`
- `tests/benchmarks/test_workspace_hot_paths.py`
- `implementation-plan/20260615-011753/stages/STAGE-009-workspace-performance-baseline.md`
- `implementation-plan/20260615-011753/stage-status.md`
- `implementation-plan/20260615-011753/change-log.md`
- `implementation-plan/20260615-011753/stage-reports/STAGE-009-20260615-161853.md`

## Benchmark baseline

Environment:
- Python: `3.12.3`
- Kernel: `Linux 6.17.0-35-generic`
- CPU: `Intel(R) Core(TM) i9-14900HX`, 8 visible CPUs, VMware hypervisor
- Filesystem for repo and `/tmp`: `ext4` on `/dev/sda2`
- Benchmark runner: `/tmp/exphttp-stage009-bench-venv/bin/python`
- `pytest-benchmark`: `5.2.3`
- Dataset sizes: 256 upload files, 256 INFO files, 128 baseline notes, 64 WebSocket slots, 20 rounds, 1 iteration per round

Command:

```text
PATH=/tmp/exphttp-stage009-bench-venv/bin:$PATH python -m pytest tests/benchmarks --benchmark-only
```

Results from the final run:

| Benchmark | Mean | Median | Rounds |
|---|---:|---:|---:|
| `test_upload_publish_no_limit_policy` | 619.5395 us | 570.7995 us | 20 |
| `test_chunked_request_receive_loop` | 868.0697 us | 873.9000 us | 20 |
| `test_websocket_slot_saturation` | 3,008.0836 us | 2,953.7990 us | 20 |
| `test_info_large_directory_listing` | 5,548.8725 us | 5,371.0975 us | 20 |
| `test_notepad_save_near_count_limit` | 7,592.6619 us | 7,363.4970 us | 20 |
| `test_upload_quota_scan_with_aggregate_limits` | 10,327.8754 us | 10,020.0960 us | 20 |
| `test_notepad_list_near_limit` | 38,410.9636 us | 38,433.7335 us | 20 |

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Initial benchmark check | `python -m pytest tests/benchmarks --benchmark-only` | FAIL | Base interpreter lacked `pytest-benchmark`, so `--benchmark-only` was unknown. |
| Benchmark environment setup | `python -m venv --system-site-packages /tmp/exphttp-stage009-bench-venv` and `/tmp/exphttp-stage009-bench-venv/bin/python -m pip install 'pytest-benchmark>=5.0'` | PASS | Temporary environment outside the repo; installed `pytest-benchmark 5.2.3`. |
| Focused upload tests | `python -m pytest tests/test_handlers/test_files.py` | PASS | `18 passed`; includes no-limit and limited quota regression coverage. |
| Required storage/INFO tests | `python -m pytest tests/test_handlers/test_files.py tests/test_handlers/test_handler_integration.py` | PASS | `117 passed`. |
| Metrics/live tests | `python -m pytest tests/test_metrics.py tests/test_server_live.py` | PASS | `30 passed`. |
| Optional-plugin behavior | `python -m pytest tests/benchmarks` | PASS | Base interpreter collects 7 benchmark tests and skips them when `pytest-benchmark` is absent. |
| Benchmark collection | `PATH=/tmp/exphttp-stage009-bench-venv/bin:$PATH python -m pytest tests/benchmarks --benchmark-only` | PASS | `7 passed`; baseline table recorded above. |
| Lint | `python -m ruff check src/storage.py tests/test_handlers/test_files.py tests/benchmarks/test_workspace_hot_paths.py` | PASS | Touched Python files passed. |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors. |
| Manual/static review | Inspect final diff | PASS | No API/INFO response semantics changed; no docs/workflows changed. |

## Outcome
- Status: CLOSED
- Completed at: 2026-06-15 16:40:18 +0300
- Remaining risks: benchmark timings are local and machine-dependent; no thresholds were added.
