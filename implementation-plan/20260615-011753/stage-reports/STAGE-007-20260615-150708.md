# STAGE-007 Closure Report
_Generated: 2026-06-15 15:07:08 +0300_

## Stage target
- Stage: STAGE-007 - Python 3.14 readiness
- Requested by: $close-plan-stage STAGE-007 --from implementation-plan/20260615-011753/stages/STAGE-007-python-314-readiness.md --with-subagents
- Active plan: `implementation-plan/20260615-011753`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was empty)
- Dependencies: satisfied (`STAGE-001` is CLOSED)

## Execution approach
1. Verify Python 3.14 readiness locally before widening package metadata by using the existing `<3.14` metadata with an explicit `--ignore-requires-python` install in a temporary Python 3.14 environment.
2. Add Python 3.14 to the constrained CI path and ensure the 3.14 path covers `pip check`, import smoke, package/wheel smoke, and security audit.
3. If the Python 3.14 smoke path passes, widen package metadata to `<3.15`, add the 3.14 classifier, and update Python support documentation plus generated docs mirrors.
4. Run targeted docs/package/static checks, record any unavailable local CI-equivalent checks, and complete a focused subagent review.

## Acceptance criteria from plan
- [x] Python 3.14 runs in CI or a readiness job using `constraints/ci.txt`.
- [x] `pip check`, import smoke, package smoke, and security audit are clean under the 3.14 path before metadata is widened.
- [x] `pyproject.toml` and docs are updated only after 3.14 readiness is evidenced.
- [x] No unrelated dependency refresh or interpreter deprecation is included.

## Documentation source checks
- Context7 resolved `actions/setup-python` as `/actions/setup-python`.
- Context7 confirmed `python-version` matrix values, `cache: pip`, `cache-dependency-path`, and `allow-prereleases` support for setup-python workflows.

## Subagents
- `devops-engineer` (`019ecb30-3370-7110-8e5a-900979197c6f`) inspected the scoped CI/security/release/package/docs surface read-only. It found no existing Python 3.14 readiness path, confirmed docs mirror behavior, and recommended a dedicated constrained Python 3.14 readiness job plus metadata/docs widening only after evidence.

## Files changed
- `.github/workflows/ci.yml` - added Python 3.14 to the constrained test matrix and added a dedicated Python 3.14 package/security smoke job.
- `.github/workflows/security.yml` - expanded `pip-audit` to run under Python 3.12 and 3.14.
- `pyproject.toml` - widened `requires-python` to `<3.15`, added the Python 3.14 classifier, and declared `setuptools` in the dev extra for tests that exercise setuptools package discovery.
- `constraints/ci.txt` - bumped only `pip` from 26.1.1 to 26.1.2 because `pip-audit` flagged `PYSEC-2026-196` in the 3.14 readiness path.
- `README.md`, `CONTRIBUTING.md`, `docs/index.md`, `CHANGELOG.md`, `docs/changelog.md`, `docs/contributing.md` - updated Python support/readiness documentation and regenerated root-canonical docs mirrors.
- `src/cli.py`, `src/handlers/__init__.py` - applied mechanical ruff fixes required for the expanded CI matrix to remain green under the pinned lint configuration.
- `implementation-plan/20260615-011753/stages/STAGE-007-python-314-readiness.md` - marked the stage closed and recorded completion notes.
- `implementation-plan/20260615-011753/stage-status.md` - updated STAGE-007 status.
- `implementation-plan/20260615-011753/change-log.md` - added STAGE-007 closure entry.
- `implementation-plan/20260615-011753/stage-reports/STAGE-007-20260615-150708.md` - recorded this closure report.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Python 3.14 availability | `uv python list 3.14` | PASS | Found `cpython-3.14.4` at `/home/user/.local/share/uv/python/cpython-3.14-linux-x86_64-gnu/bin/python3.14`. |
| Pre-metadata 3.14 install smoke | `uv venv --seed --python ... /tmp/exphttp-stage007-py314 && PIP_CONSTRAINT=constraints/ci.txt ... pip install --ignore-requires-python -e ".[dev,lint,test]" build pip-audit && ... pip check && ... import acme, cryptography, josepy` | PARTIAL | Install, `pip check`, and import smoke passed before metadata was widened. `pip-audit` failed on `pip==26.1.1` (`PYSEC-2026-196`). |
| Python 3.14 full readiness | `uv venv --seed --python ... /tmp/exphttp-stage007-py314 && PIP_CONSTRAINT=constraints/ci.txt ... pip install -e ".[dev,lint,test]" build pip-audit && ... pytest -q && ... pip_audit --strict --no-deps -r constraints/ci.txt && ... build ... && installed-wheel smoke` | PASS | After the targeted `pip` pin fix and `setuptools` dev-extra declaration: `851 passed`, no vulnerabilities, wheel/sdist built, installed-wheel import and CLI smoke passed outside the checkout. |
| Docs mirror check | `python tools/sync_docs.py --check` | PASS | Documentation mirrors are in sync. |
| Stale docs guard | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
| Ambient current Python baseline | `python -m pytest`; `python -m exphttp --help && python -c "import acme, cryptography, josepy; from OpenSSL import SSL"` | FAIL | Ambient Python 3.12 lacks declared test/runtime dependencies and has unrelated global packages. Verified the current interpreter in a clean venv instead. |
| Clean Python 3.12 current-version lane | `uv venv --seed --python python3.12 /tmp/exphttp-stage007-py312 && PIP_CONSTRAINT=constraints/ci.txt ... pip install -e ".[dev,lint,test]" && ... pytest -q` | PASS | Clean Python 3.12 install, `pip check`, import smoke, constraints check, and `851 passed`. |
| Ruff check | `/tmp/exphttp-stage007-py314/bin/ruff check exphttp src tests` | PASS | Initial run found pre-existing B010 issues in `src/cli.py`; fixed mechanically and reran successfully. |
| Ruff format check | `/tmp/exphttp-stage007-py314/bin/ruff format --check exphttp src tests` | PASS | Initial run required formatting `src/handlers/__init__.py`; fixed mechanically and reran successfully. |
| Mypy | `/tmp/exphttp-stage007-py314/bin/mypy exphttp src` | PASS | No issues in 42 source files. |
| Workflow YAML parse | `/tmp/exphttp-stage007-py314/bin/python - <<'PY' ... yaml.safe_load(...) ... PY` | PASS | `.github/workflows/ci.yml` and `.github/workflows/security.yml` parse. |
| Targeted post-ruff tests, Python 3.14 | `/tmp/exphttp-stage007-py314/bin/python -m pytest -q tests/test_cli.py tests/test_handler_registry.py tests/test_server_methods.py` | PASS | `237 passed`. |
| Targeted post-ruff tests, Python 3.12 | `/tmp/exphttp-stage007-py312/bin/python -m pytest -q tests/test_cli.py tests/test_handler_registry.py tests/test_server_methods.py` | PASS | `237 passed`. |
| Final package/security smoke | `/tmp/exphttp-stage007-py314/bin/python -m pip_audit --strict --no-deps -r constraints/ci.txt && /tmp/exphttp-stage007-py314/bin/python -m build --sdist --wheel --outdir /tmp/exphttp-stage007-dist && ... installed-wheel smoke` | PASS | No vulnerabilities; package built; installed wheel passed `pip check`, CLI, and source-tree import guard. |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors. |

## CI workflow review
- Main CI test matrix now includes Python 3.14 while preserving Python 3.10 and 3.11 support.
- A dedicated `python314-readiness` job runs constrained install, `pip check`, import smoke, dependency constraint validation, `pip-audit`, sdist/wheel build, and installed-wheel smoke under Python 3.14.
- The security workflow now audits pinned constraints under both Python 3.12 and 3.14.
- Release artifact workflow remains unchanged; the new CI readiness job covers Python 3.14 package smoke before release policy changes.

## Outcome
Status: CLOSED

All acceptance criteria are met. The only dependency pin change is the targeted `pip` 26.1.2 security fix required for the 3.14 `pip-audit` gate; no broad dependency refresh, Docker base change, or Python 3.10/3.11 deprecation was included.
