# STAGE-002 Closure Report
_Generated: 2026-05-05 21:34:22 MSK_

## Stage target
- Stage: STAGE-002 - Fix Secure Notepad key contract and example interoperability
- Requested by: `$close-plan-stage STAGE-002, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-001` is `CLOSED`)

## Execution approach
1. Confirm canonical Secure Notepad HKDF constants and current example/browser/server behavior.
2. Update the example, docs, and regression coverage so they agree on the key contract and current recovery limits.
3. Run targeted tests, example sanity, docs sync, static review, and verification subagents before updating plan status.

## Acceptance criteria from plan
- [x] `examples/notepad_client.py` uses the same HKDF salt/info as server and browser.
- [x] A regression test fails on the old example constants and passes with the fixed constants.
- [x] `API.md` documents the current session-key-bound recoverability limitation without promising durable decryptability.
- [x] `API.md` states that note body `data` is encrypted and title metadata is plaintext/server-visible.
- [x] `docs/api.md` is synchronized with `API.md`.

## Changes made
- `examples/notepad_client.py`: replaced the old HKDF `salt=None` and `exphttp-notepad` info with the canonical 32 zero-byte salt and `notepad-e2e-key`.
- `tests/test_security/test_keys.py`: added focused interoperability coverage proving the fixed example key matches the server-derived key and the old constants do not.
- `API.md`: documented exact Secure Notepad ECDH/HKDF/AES-GCM parameters, plaintext metadata, and session-bound recovery limits.
- `docs/api.md`: regenerated from `API.md` with `tools/sync_docs.py --write`.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline mismatch | `.venv/bin/python - <<'PY' ... old example/server check ... PY` | PASS | Before edits, `old_example_matches_server=False`; system `python` lacked project runtime deps, so verification used `.venv`. |
| Focused regression | `.venv/bin/pytest -q tests/test_security/test_keys.py -k notepad_client_example` | PASS | 1 passed; proves old constants do not match and fixed example key does match. |
| Targeted tests | `.venv/bin/pytest -q tests/test_security/test_keys.py tests/test_handlers/test_notepad.py tests/test_websocket_handlers.py` | PASS | 88 passed. |
| Example sanity | `.venv/bin/python examples/notepad_client.py --help > /dev/null` | PASS | Exited 0. |
| Docs sync | `.venv/bin/python tools/sync_docs.py --check` | PASS | Documentation mirrors are in sync. |
| Lint | `.venv/bin/ruff check examples/notepad_client.py tests/test_security/test_keys.py` | PASS | All checks passed. |
| Static docs review | `rg -n "HKDF salt|notepad-e2e-key|session-bound|plaintext metadata|opaque base64 blob|decrypt or re-key" API.md docs/api.md` | PASS | Exact HKDF salt/info, recovery limits, encrypted data, and plaintext metadata text are present in both docs. |
| Diff check | `git diff --check` | PASS | No whitespace errors. |
| Compile check | `.venv/bin/python -m py_compile examples/notepad_client.py tests/test_security/test_keys.py tools/sync_docs.py` | PASS | Generated bytecode was removed afterward. |
| Explorer subagent | `explorer` read-only key-contract map | PASS | Confirmed server/browser canonical constants and recommended the implemented scope. |
| Correctness subagent | `reviewer` verification review | PASS | No product-code/docs regressions; only requested final plan-artifact updates. |
| Security subagent | `security-auditor` verification review | PASS | No blocking findings; residual risk limited to no live temp-server/browser round trip. |

## Subagent summary
- Explorer confirmed server/browser already aligned on HKDF-SHA256, 32 zero-byte salt, `notepad-e2e-key`, and identified the example/docs/test gaps.
- Reviewer confirmed implementation acceptance criteria are satisfied once plan artifacts are updated.
- Security auditor confirmed crypto-contract consistency and that public docs do not overpromise recovery or hide plaintext metadata limits.

## Residual risk
- No live temp-server example round trip or browser WebCrypto runtime test was run. The stage-required static review plus key, HTTP NOTE, and WebSocket handler tests passed.
- Existing notes created by the old example constants remain incompatible unless the user still has the old derived key and re-saves.

## Final status
- Result: CLOSED
- Completed: 2026-05-05 21:43:24 MSK
