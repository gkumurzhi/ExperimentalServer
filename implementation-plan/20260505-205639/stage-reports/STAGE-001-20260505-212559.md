# STAGE-001 Closure Report
_Generated: 2026-05-05 21:25:59 MSK_

## Stage target
- Stage: STAGE-001 - Restore release-smoke stale-doc guard
- Requested by: `$close-plan-stage STAGE-001, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; stage has no dependencies
- Current blocker evidence: the release-smoke grep from `.github/workflows/ci.yml` matches `docs/changelog.md:38`, a historical changelog entry for `advanced upload is enabled by default`, and exits before browser/Docker smoke can run.

## Execution approach
1. Replace the brittle inline recursive grep with a small checked Python script that separates active documentation inputs from historical changelog files.
2. Keep actionable deny patterns for active docs/examples, with clear messages showing the matched file and stale phrase.
3. Add focused tests for the checker so historical changelog text is allowed while active stale text still fails.
4. Update the CI workflow to call the checker, then run targeted verification and a subagent review.

## Acceptance criteria from plan
- [x] The known `advanced upload is enabled by default` changelog/history entry no longer fails release smoke.
- [x] Active stale references are still detected in active docs or examples.
- [x] The workflow remains readable and fails with an actionable message.
- [x] Browser and Docker smoke steps are no longer blocked by the stale-doc guard.

## Implementation summary
- Replaced the inline recursive grep in `.github/workflows/ci.yml` with `python tools/check_stale_docs.py`.
- Added `tools/check_stale_docs.py`, which scans active docs/examples and intentionally skips historical changelog files.
- Added focused regression tests in `tests/test_check_stale_docs.py`.

## Files changed
- `.github/workflows/ci.yml` - calls the checked stale-doc guard script in release smoke.
- `tools/check_stale_docs.py` - new active-doc stale contract scanner.
- `tests/test_check_stale_docs.py` - regression coverage for historical changelog allowance and active stale-reference failures.
- `implementation-plan/20260505-205639/stage-reports/STAGE-001-20260505-212559.md` - closure report.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline false positive | `grep -R -n -E -- '--root|--max-upload|/notes/pubkey|X-Enc-Key|X-HMAC|--no-info|--opsec|--sandbox|advanced upload is enabled by default|ciphertext \+ metadata' README.md API.md docs examples CLAUDE.md` | PASS | Reproduced old blocker: only `docs/changelog.md:38` matched. |
| Stale guard | `.venv/bin/python tools/check_stale_docs.py` | PASS | Current active docs/examples have no stale contract matches; historical changelog text is skipped. |
| Targeted tests | `.venv/bin/python -m pytest -q tests/test_check_stale_docs.py` | PASS | 3 tests passed. |
| Lint | `.venv/bin/ruff check tools/check_stale_docs.py tests/test_check_stale_docs.py` | PASS | No ruff findings after fixing one line-length issue. |
| CLI version | `.venv/bin/python -m src --dir /tmp --version` | PASS | Printed `exphttp 2.0.0`. |
| CLI help | `.venv/bin/python -m src --help` | PASS | Help rendered successfully. |
| Example help | `.venv/bin/python examples/notepad_client.py --help` | PASS | Help rendered successfully. |
| Release-smoke sanity block | `set -euo pipefail; .venv/bin/python -m src --dir /tmp --version; .venv/bin/python -m src --help > /dev/null; bash -n examples/basic_file_server.sh; .venv/bin/python examples/notepad_client.py --help > /dev/null; .venv/bin/python tools/check_stale_docs.py` | PASS | Confirms the release-smoke command sequence reaches past the stale-doc guard locally. |
| System interpreter note | `python -m src --help`; `pytest -q tests/test_check_stale_docs.py` | FAIL | System Python lacks repo dependencies (`acme`, `pytest`); reran required checks with `.venv/bin/python`, which is dependency-complete. |

## Subagent verification
- `explorer` confirmed the old CI guard matched only `docs/changelog.md:38` among guarded paths and found no active-doc stale matches when changelog/history was excluded.
- `devops-engineer` confirmed the checker preserves the previous deny patterns, allows changelog/history intentionally, emits actionable failure output, and satisfies the behavioral acceptance criteria.

## Outcome
- Stage result: CLOSED
- Plan status updated: yes
- Remaining risks: full GitHub release smoke was not run locally; the stage-specific stale-doc blocker and local dependency-complete sanity path pass.
