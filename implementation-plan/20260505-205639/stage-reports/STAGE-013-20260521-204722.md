# STAGE-013 Closure Report
_Generated: 2026-05-21 20:47:22 MSK_

## Stage target
- Stage: STAGE-013 - Resolve local-only pytest collection drift
- Requested by: `$close-plan-stage STAGE-013 --from implementation-plan/20260505-205639/stages/STAGE-013-ci-local-collection-drift.md --with-subagents`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; `STAGE-001` is `CLOSED`

## Execution approach
1. Confirm the local ignored runner files and current pytest/CI collection behavior.
2. Preserve the local-only runner files, but make tracked pytest collection policy ignore the known local runner test.
3. Add a tracked guard for ignored pytest-style files so future local-only tests cannot silently skew local confidence.
4. Run targeted guard tests, collect-only, and a sanity test subset; then update plan artifacts.

## Acceptance criteria from plan
- [x] `pytest --collect-only` no longer collects tests that are ignored and absent from CI checkout.
- [x] The repo has an explicit policy for local Codex runner files.
- [x] CI/local confidence drift from ignored test collection is prevented.
- [x] No local-only files are deleted or moved without a clear decision.
- [x] Full project test collection still includes all intended tracked tests.

## Documentation check
- Used Context7 for current pytest collection-ignore guidance. `/pytest-dev/pytest` documents `collect_ignore` / `collect_ignore_glob` in `conftest.py`; `collect_ignore` accepts path strings or `pathlib.Path` values.

## Subagent review
- `explorer` reviewed `.gitignore`, `pyproject.toml`, `.github/workflows/ci.yml`, `tools/close_plan_stages.py`, and `tests/test_close_plan_stages.py`.
- Finding: the close-plan runner pair is local agent-only tooling: it shells out to `codex exec`, uses `$close-plan-stage`, writes runner logs under Git metadata, and performs commit/push orchestration.
- Recommendation: keep the runner pair ignored, explicitly exclude `tests/test_close_plan_stages.py` from project collection, and add a tracked guard for ignored pytest-style tests.

## Implementation summary
- Preserved the ignored local runner files without tracking, deleting, or moving them.
- Clarified the `.gitignore` local Codex runner policy.
- Added `collect_ignore` for `tests/test_close_plan_stages.py` in `tests/conftest.py` so the allowed local runner test is absent from project pytest collection.
- Added `tools/check_pytest_collection_policy.py` to detect ignored pytest-style files under `tests/`, allowing only the reviewed local runner test.
- Added `tests/test_pytest_collection_policy.py` coverage for the allowlist and synthetic unapproved ignored test behavior.
- Added the policy helper to CI after dependency smoke.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline collect-only | `pytest --collect-only -q -p no:cacheprovider` | FAIL | Outer Python lacks `acme`; this is an environment dependency issue before collection. Project `.venv` has required deps. |
| Context7 pytest docs | `resolve pytest` / `query collect_ignore` | PASS | Confirmed supported `collect_ignore` usage. |
| Subagent inspection | `explorer` read-only review | PASS | Confirmed local-agent ownership decision and recommended ignored local policy. |
| Collect-only | `.venv/bin/pytest --collect-only -q -p no:cacheprovider > /tmp/stage013-collect.txt` | PASS | 671 tests collected. |
| Ignored local test absence | `rg -n "test_close_plan_stages" /tmp/stage013-collect.txt` | PASS | No matches; command exited 1 as expected for absence. |
| Ignored pytest file inventory | `git ls-files --others --ignored --exclude-standard -- 'tests/test_*.py' 'tests/**/test_*.py'` | PASS | Only `tests/test_close_plan_stages.py` is ignored and pytest-style. |
| Policy helper | `.venv/bin/python tools/check_pytest_collection_policy.py` | PASS | Reports the known local runner test as explicitly allowed. |
| Guard tests | `.venv/bin/pytest -q tests/test_pytest_collection_policy.py` | PASS | 3 passed. |
| Sanity tests | `.venv/bin/pytest -q tests/test_cli.py` | PASS | 62 passed. |
| Ruff lint | `.venv/bin/ruff check tools/check_pytest_collection_policy.py tests/test_pytest_collection_policy.py tests/conftest.py` | PASS | All checks passed. |
| Ruff format | `.venv/bin/ruff format --check tools/check_pytest_collection_policy.py tests/test_pytest_collection_policy.py tests/conftest.py` | PASS | 3 files already formatted after formatter pass. |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors. |

## Files changed
- `.github/workflows/ci.yml` - run the ignored pytest collection policy in CI.
- `.gitignore` - document that local Codex runner files are operator-local and excluded from project pytest collection.
- `tests/conftest.py` - exclude the known local runner test from project collection.
- `tools/check_pytest_collection_policy.py` - add the tracked ignored pytest-style file guard.
- `tests/test_pytest_collection_policy.py` - add guard regression tests.
- `implementation-plan/20260505-205639/stages/STAGE-013-ci-local-collection-drift.md` - mark stage closed and record completion notes.
- `implementation-plan/20260505-205639/stage-status.md` - update stage status.
- `implementation-plan/20260505-205639/change-log.md` - append closure entry.
- `implementation-plan/20260505-205639/stage-reports/STAGE-013-20260521-204722.md` - closure report.

## Result
CLOSED
