# STAGE-005 Closure Report
_Generated: 2026-05-05 22:32:34 MSK_

## Stage target
- Stage: STAGE-005 - Complete container ACME/sslip operator path
- Requested by: `$close-plan-stage STAGE-005, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-003` is `CLOSED`)
- Context7: checked Docker Compose docs for service profiles, port mappings, named volumes, and `healthcheck.disable`.

## Execution approach
1. Inspect the current Dockerfile, Compose example, README TLS/ACME guidance, MkDocs pages, and ACME implementation paths.
2. Keep the default plain HTTP Compose service intact, then add an opt-in ACME/sslip Compose profile with explicit host/container ports, writable ACME state, and image healthcheck override.
3. Document container/NAT prerequisites, `api.ipify.org` public IPv4 detection, ACME cache sensitivity and repair, unsupported wildcard/DNS-01 behavior, and TLS/auth/ACME healthcheck limitations.
4. Run Compose config, MkDocs strict build, docs-sync check, and static comparison against `src/security/tls.py` and `src/security/tls_manager.py`.
5. Run verification subagents for Docker/ops posture and final correctness review.

## Acceptance criteria from plan
- [x] Operators can identify the exact ports and volume needed for ACME/sslip in containers.
- [x] The default plain HTTP container path remains intact.
- [x] TLS/auth/ACME healthcheck limitations are called out or handled.
- [x] ACME cache repair guidance aligns with STAGE-003 behavior.
- [x] Docs do not imply live ACME works behind NAT/private IPv4 without public port 80 reachability.

## Implementation summary
- `Dockerfile`: pre-created `/home/exphttp/.exphttp` with runtime-user ownership so the ACME state volume has a non-root mount point.
- `examples/docker/docker-compose.yml`: kept the default `exphttp` service on `8080:8080`; added opt-in `exphttp-acme` profile with host `80` -> container `8080`, host `443` -> container `8443`, `exphttp-acme-state:/home/exphttp/.exphttp`, `--sslip`, `--acme-http-port 8080`, and disabled image healthcheck.
- `README.md`: added a Docker ACME/sslip operator recipe covering exact ports, volume, NAT/public IPv4 requirements, `api.ipify.org`, staging, domain alternative, unsupported wildcard/DNS-01, cache repair, and healthcheck limitations.
- `docs/architecture.md`: documented runtime persistence boundaries for uploads, notes, temporary self-signed certs, ACME state, legacy cache reads, and Docker ACME state volume.

## Files changed
- `Dockerfile`
- `examples/docker/docker-compose.yml`
- `README.md`
- `docs/architecture.md`
- `implementation-plan/20260505-205639/stages/STAGE-005-container-acme-sslip-guidance.md`
- `implementation-plan/20260505-205639/stage-status.md`
- `implementation-plan/20260505-205639/change-log.md`
- `implementation-plan/20260505-205639/stage-reports/STAGE-005-20260505-223234.md`

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Context7 docs check | Resolve/query Docker Compose docs for profiles, ports, named volumes, healthcheck disable | PASS | Confirmed `profiles`, `ports`, named volume syntax, and `healthcheck: disable: true` from Docker docs. |
| Compose validation | `docker compose -f examples/docker/docker-compose.yml config` | PASS | Default profile resolves and includes only the existing plain HTTP `exphttp` service on `8080:8080`. |
| Compose ACME validation | `docker compose -f examples/docker/docker-compose.yml --profile acme config` | PASS | ACME profile resolves with `80:8080`, `443:8443`, `exphttp-acme-state`, and disabled healthcheck. |
| Docs build | `mkdocs build --strict` | FAIL | `mkdocs` was not on PATH for the system Python. Retried with project virtualenv. |
| Docs build | `.venv/bin/mkdocs build --strict` | PASS | Strict MkDocs build completed; Material emitted its upstream MkDocs 2.0 advisory but the command exited 0. |
| Docs sync | `python tools/sync_docs.py --check` | PASS | Generated mirror docs are in sync; no generated mirror file was edited. |
| Dockerfile check | `docker build --check -f Dockerfile .` | PASS | BuildKit Dockerfile checks completed with no warnings. |
| Static ops review | `rg -n "DEFAULT_ACME_HTTP_PORT|DEFAULT_PUBLIC_IP_URL|Path\\.home|validate_public_ipv4|wildcard|DNS-01|acme_http_port|api\\.ipify|healthcheck|80:8080|443:8443|exphttp-acme-state|/home/exphttp/.exphttp|--acme-http-port|--sslip|--letsencrypt" ...` | PASS | Docs and Compose line up with `DEFAULT_ACME_HTTP_PORT = 80`, `DEFAULT_PUBLIC_IP_URL = "https://api.ipify.org"`, `Path.home()/.exphttp/acme`, public IPv4 validation, and wildcard/DNS-01 rejection. |
| Diff hygiene | `git diff --check` | PASS | No whitespace errors. |
| Explorer review | Subagent `explorer` | PASS | Confirmed ports, state path, `api.ipify.org`, global IPv4, HTTP-01-only behavior, and STAGE-003 cache repair semantics; recommended wording fixes were applied. |
| Docker/ops review | Subagent `docker-expert` | PASS | Verified default and ACME Compose configs, docs sync, diff hygiene, and no acceptance-blocking findings. |
| Correctness review | Subagent `reviewer` | PASS | Verified final diff and `.venv/bin/mkdocs build --strict`; no blocking source/docs findings. |

## Subagent verification
- `explorer`: PASS. Confirmed the planned scope was sufficient, with operational residual risk limited to live public ACME/NAT conditions outside this repo. Recommended replacing the non-routable sample IP and clarifying HTTP-01-only/cache repair wording; these changes were applied.
- `docker-expert`: PASS. Confirmed the default service still exposes only `8080:8080`, the ACME profile exposes `80:8080` and `443:8443`, mounts `exphttp-acme-state:/home/exphttp/.exphttp`, disables the HTTP-only healthcheck, and documents the required caveats.
- `reviewer`: PASS. Found no blocking source/docs issues and confirmed the stage can be marked `CLOSED`. Residual live ACME/TLS container smoke is deferred to STAGE-008 per the plan.

## Final status
- Result: CLOSED
- Closed at: 2026-05-05 22:41:47 MSK
- Plan artifacts updated: yes
- Commit/push: not performed per user instruction.
