# STAGE-008 Closure Report
_Generated: 2026-05-05 23:34:11 MSK_

## Stage target
- Stage: STAGE-008 - Add Docker TLS and runtime import smoke
- Requested by: `$close-plan-stage STAGE-008, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none (`git status --short` was clean)
- Dependencies: satisfied (`STAGE-003` and `STAGE-005` are `CLOSED`)

## Execution approach
1. Inspect the existing Docker smoke, image entrypoint, and healthcheck behavior.
2. Extend the release-smoke Docker step with an image-level runtime dependency import check.
3. Add a self-signed TLS container smoke that disables the HTTP-only image healthcheck and probes HTTPS PING with `curl -k`.
4. Run workflow syntax/static checks plus local Docker build/run verification if Docker is available.
5. Run verifier subagents, reconcile findings, and update plan artifacts.

## Acceptance criteria from plan
- [x] Docker CI fails if the image cannot import required ACME/crypto runtime packages.
- [x] Docker CI proves self-signed TLS mode responds to HTTPS PING.
- [x] Existing plain HTTP Docker smoke remains present.
- [x] The smoke does not require live ACME, public network ingress, or secrets.
- [x] Healthcheck behavior does not create false negatives for the TLS smoke container.

## Implementation summary
- Updated `.github/workflows/ci.yml` Docker smoke to run a built-image import check with `--entrypoint python` for `acme`, `cryptography`, `josepy`, and `OpenSSL`.
- Preserved the existing Docker build, image `--version`, and plain HTTP PING smoke.
- Added a second Docker run in self-signed `--tls` mode and probes it with `curl -k -X PING`.
- Bound smoke containers to ephemeral `127.0.0.1` host ports and discovered those ports with `docker port` to avoid fixed-port collisions.
- Added `curl --noproxy '*'` so local container probes bypass any proxy environment.
- Disabled the baked HTTP-only image healthcheck only for the TLS smoke container with `--no-healthcheck`.
- Tightened `probe_ping` so both the `curl` command and the `"status": "pong"` body assertion must pass before returning success.

## Files changed
- `.github/workflows/ci.yml` - Docker release-smoke coverage.
- `implementation-plan/20260505-205639/stages/STAGE-008-docker-tls-runtime-smoke.md` - stage status and completion notes.
- `implementation-plan/20260505-205639/stage-status.md` - stage table status.
- `implementation-plan/20260505-205639/change-log.md` - closure changelog entry.
- `implementation-plan/20260505-205639/stage-reports/STAGE-008-20260505-233412.md` - this report.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline status | `git status --short` | PASS | Worktree was clean before edits. |
| Workflow YAML parse | `python - <<'PY' ... yaml.safe_load(Path('.github/workflows/ci.yml').read_text()) ... PY` | PASS | Docker smoke step read from parsed YAML. |
| Workflow shell syntax | `python - <<'PY' ... print(step['run']) ... PY \| bash -n` | PASS | Final Docker smoke run block parses as Bash. |
| Host CLI smoke | `python -m src --help >/dev/null` | FAIL (environment) | Local host environment lacks `acme`; not used as stage evidence because this stage verifies the Docker runtime install path. |
| Initial Docker smoke | `python - <<'PY' ... print(step['run']) ... PY \| bash` | FAIL (fixed) | Build, imports, and HTTP PING passed; HTTPS probe failed because local `HTTPS_PROXY` captured `127.0.0.1`. Fixed with `curl --noproxy '*'`. |
| Docker cleanup | `docker rm -f <leftover smoke containers>` | PASS | Removed containers left from interrupted failed smoke attempts. |
| Port collision retry | `python - <<'PY' ... print(step['run']) ... PY \| bash` | FAIL (fixed) | Rerun hit local fixed port `18082`; fixed by using ephemeral localhost ports and `docker port`. |
| Full Docker smoke | `python - <<'PY' ... print(step['run']) ... PY \| bash` | PASS | Built `exphttp:ci`, imported runtime crypto/ACME dependencies, ran `--version`, plain HTTP PING, and self-signed HTTPS PING. |
| Final Docker smoke after assertion fix | `python - <<'PY' ... print(step['run']) ... PY \| bash` | PASS | Re-ran after making the pong `grep` part of the success condition. HTTP and TLS probes returned `"status": "pong"`. |
| Container cleanup check | `docker ps -a --format '{{.ID}} {{.Image}} {{.Status}} {{.Ports}} {{.Names}}' \| rg 'exphttp:ci\|18082\|18443\|32769'` | PASS | No matching smoke containers remained. |
| Diff whitespace | `git diff --check -- .github/workflows/ci.yml` | PASS | No whitespace errors. |
| Final diff review | `git diff -- .github/workflows/ci.yml` | PASS | Diff matches stage scope. |

## Subagent verification
- Explorer inspected `.github/workflows/ci.yml`, `Dockerfile`, CLI/TLS code, and PING handler behavior. It recommended an entrypoint override for Python imports, `--tls` self-signed smoke, `curl -k`, and `--no-healthcheck` for TLS mode.
- Reviewer found a Bash false-positive path where `grep` could fail under a negated `if ! probe_ping ...` call while the function still returned success. The workflow was corrected so `curl | tee` and `grep '"status": "pong"'` are in the same success condition.
- Docker verifier independently found the same assertion issue, then re-reviewed the fixed diff and reported no blocking findings. It confirmed imports, plain HTTP PING, self-signed TLS PING, `--no-healthcheck`, proxy bypass, and ephemeral localhost ports.

## Result
- Status: CLOSED
- Closed at: 2026-05-05 23:44:54 MSK
- Plan artifacts updated: yes

