# STAGE-007 Closure Report
_Generated: 2026-05-05 23:12:28 MSK_

## Stage target
- Stage: STAGE-007 - Align pre-commit and dependency completeness checks
- Requested by: `$close-plan-stage STAGE-007, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-006` is `CLOSED`)
- Prior STAGE-007 reports: none

## Execution approach
1. Compare isolated mypy hook dependencies against `constraints/ci.txt` and the imports in `src/security/tls.py`.
2. Align the pre-commit mypy hook with the constrained runtime ACME/crypto graph.
3. Add CI and security workflow checks that run after package install and prove the installed runtime graph is complete.
4. Update local contributor guidance and regenerate the docs mirror if root-canonical docs change.
5. Run targeted verification, then run verification subagents for independent review.

## Acceptance criteria from plan
- [x] Pre-commit mypy installs the packages imported by `src/security/tls.py`.
- [x] The pinned `cryptography` version used by the mypy hook matches the declared lower-bound policy and current constraint.
- [x] CI catches missing runtime dependencies after package install.
- [x] Security/audit workflow either audits installed completeness or documents a clear constraints completeness check.
- [x] Local setup guidance does not ask developers to rely on stale optional extras for required runtime dependencies.

## Documentation check
- Context7 `/pre-commit/pre-commit.com`: verified `additional_dependencies` installs extra packages into the hook environment.
- Context7 `/pypa/pip-audit`: verified `pip-audit -r` audits requirement files, `pip-audit` audits the current environment, and `--no-deps` asserts fully pinned input.

## Implementation log
- Updated `.pre-commit-config.yaml` mypy `additional_dependencies` to install `acme==5.5.0`, `cryptography==48.0.0`, `josepy==2.2.0`, and `PyOpenSSL==26.2.0`.
- Added CI dependency completeness smoke after constrained package install in the Python matrix and cross-platform jobs.
- Updated `security.yml` to install the package before audit, run `pip check`, CLI/import smoke, and the new constraints-vs-installed checker before strict pinned `pip-audit`.
- Added `tools/check_dependency_constraints.py` to verify installed third-party distributions match pinned versions in `constraints/ci.txt`, ignoring only the editable local `exphttp` package by default.
- Updated `CONTRIBUTING.md` and regenerated `docs/contributing.md` with the local dependency completeness checks.
- Local system Python could not be modified because it is PEP 668 externally managed; verification used the existing `.venv` and clean temporary virtual environments.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline installed smoke | `python -m pip check`; `python -m src --help`; `python -c "import acme, cryptography, josepy; from OpenSSL import SSL"` | FAIL/PASS | System Python lacked `acme`; `.venv` and clean constrained envs passed after using the pinned install path. |
| Pre-commit mypy | `.venv/bin/pre-commit run mypy --all-files` | PASS | Refreshed hook environment installed ACME/crypto pins. |
| Installed env smoke | `.venv/bin/python -m pip check`; `.venv/bin/python -m src --help > /dev/null`; `.venv/bin/python -c "import acme, cryptography, josepy; from OpenSSL import SSL"` | PASS | Runtime import boundary present. |
| Constraint completeness | `.venv/bin/python tools/check_dependency_constraints.py --constraints constraints/ci.txt` | PASS | Installed distributions match pinned versions after local `.venv` pip refresh to `26.1.1`. |
| Security audit | `.venv/bin/python -m pip_audit --strict --no-deps -r constraints/ci.txt` | PASS | Pinned constraints audit passed; `pip-audit` emitted the expected hash recommendation warning. |
| Workflow syntax | `.venv/bin/pre-commit run check-yaml --files .github/workflows/ci.yml .github/workflows/security.yml .pre-commit-config.yaml` | PASS | YAML parsed. |
| Helper lint | `.venv/bin/ruff check tools/check_dependency_constraints.py`; `.venv/bin/ruff format --check tools/check_dependency_constraints.py` | PASS | Helper is lint-clean and formatted. |
| Docs sync | `.venv/bin/python tools/sync_docs.py --check` | PASS | Generated docs mirror is in sync. |
| Stale docs guard | `.venv/bin/python tools/check_stale_docs.py` | PASS | No stale active crypto-extra guidance. |
| Clean constrained install | Temporary venv with `PIP_CONSTRAINT=constraints/ci.txt`, `pip install -e ".[dev,lint,test]"`, `pip check`, CLI/import smoke, constraints checker | PASS | Reproduces CI dependency install path. |
| Clean security job simulation | Temporary venv with `PIP_CONSTRAINT=constraints/ci.txt`, `pip install -e . pip-audit`, `pip check`, CLI/import smoke, constraints checker, strict pinned `pip-audit` | PASS | Reproduces security workflow dependency/audit path. |
| Diff hygiene | `git diff --check` | PASS | No whitespace errors. |

## Subagent verification
- `dependency-manager`: approved dependency-policy consistency; noted only the compatibility empty `crypto` extra as a non-blocking residual.
- `reviewer`: found the initial security workflow did not prove installed packages were represented in audited constraints. Fixed by adding `tools/check_dependency_constraints.py` and wiring it before `pip-audit`.
- `qa-expert`: approved closure after reviewing verification coverage; residual risk is limited to final GitHub Actions execution in the hosted environment.

## Changed files
- `.pre-commit-config.yaml`
- `.github/workflows/ci.yml`
- `.github/workflows/security.yml`
- `tools/check_dependency_constraints.py`
- `CONTRIBUTING.md`
- `docs/contributing.md`
- `implementation-plan/20260505-205639/stages/STAGE-007-precommit-dependency-completeness.md`
- `implementation-plan/20260505-205639/stage-status.md`
- `implementation-plan/20260505-205639/change-log.md`
- `implementation-plan/20260505-205639/stage-reports/STAGE-007-20260505-231228.md`

## Outcome
- Status: CLOSED
- Closed at: 2026-05-05 23:29:51 MSK
- Summary: pre-commit, CI, security audit, and contributor guidance now prove the constrained ACME/crypto runtime graph is installed, version-pinned, and audited.
