# STAGE-004 Closure Report
_Generated: 2026-05-05 22:12:47 MSK_

## Stage target
- Stage: STAGE-004 - Normalize CLI TLS source and numeric validation
- Requested by: `$close-plan-stage STAGE-004, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260505-205639`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-001` is `CLOSED`)
- Environment note: system `python` cannot import `acme`; project `.venv/bin/python` has the runtime/test dependencies and is used for verification.
- Baseline behavior:
  - `create_parser().parse_args()` accepted `--port 0`, `--port -1`, and `--port 70000`.
  - `create_parser().parse_args()` accepted `--max-size 0`, `--max-size -1`, `--workers 0`, and `--workers -1`.
  - With the server stubbed, `cli.main(["--key", "key.pem"])` returned `0` and passed `tls=False`, `key_file="key.pem"`.
  - With the server stubbed, `cli.main(["--cert", "cert.pem"])` returned `0` and passed `tls=True`, `key_file=None`.
  - With the server stubbed, `cli.main(["--cert", "cert.pem", "--key", "key.pem", "--letsencrypt", "--domain", "example.com"])` returned `0` and passed both user cert/key and ACME options.

## Execution approach
1. Add CLI validators for bounded integer flags and post-parse TLS source validation.
2. Add server constructor guards for programmatic `port`, `max_upload_size`, and `max_workers` values before side effects.
3. Add focused CLI and server invariant regression tests.
4. Run targeted tests, CLI help, safe subprocess checks, and verification subagent review.

## Acceptance criteria from plan
- [x] `--key` alone no longer starts plaintext HTTP.
- [x] `--cert` alone and `--key` alone both fail early.
- [x] user-supplied cert/key combined with ACME mode is rejected or follows an explicitly documented behavior.
- [x] `--port`, `--max-size`, and `--workers` invalid lower/out-of-range values fail at CLI parse/validation time.
- [x] Programmatic server construction rejects invalid primary limits before sockets/thread pools are created.

## Changes made
- Added bounded argparse validators for `--port`, `--max-size`, `--workers`, and `--acme-http-port`.
- Added post-parse TLS validation so cert/key must be supplied together, empty cert/key values fail, and custom cert/key cannot be combined with `--letsencrypt` or `--sslip`.
- Added server constructor guards for invalid `port`, `max_upload_size`, and `max_workers` before filesystem, socket, or thread-pool side effects.
- Added focused CLI and constructor regression tests for invalid numeric values, TLS source conflicts, empty cert/key values, and side-effect-free constructor failure.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted CLI tests | `.venv/bin/pytest -q tests/test_cli.py` | PASS | 62 passed |
| TLS manager regression tests | `.venv/bin/pytest -q tests/test_security/test_tls_manager.py` | PASS | 20 passed |
| CLI help | `.venv/bin/python -m src --help > /dev/null` | PASS | Exit 0 |
| Manual subprocess check | `.venv/bin/python -m src --key key.pem` | PASS | Exit 2 with cert/key pairing error |
| Manual subprocess check | `.venv/bin/python -m src --key ''` | PASS | Exit 2 with cert/key pairing error |
| Manual subprocess check | `.venv/bin/python -m src --workers 0` | PASS | Exit 2 with workers lower-bound error |
| Lint | `.venv/bin/ruff check src/cli.py src/server.py tests/test_cli.py` | PASS | All checks passed |
| Format | `.venv/bin/ruff format --check src/cli.py src/server.py tests/test_cli.py` | PASS | 3 files already formatted |
| Typecheck | `.venv/bin/mypy src/cli.py src/server.py` | PASS | No issues in touched source files |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors |
| Verification subagent | `explorer` code path review | PASS | Confirmed current behavior and recommended tests/guards |
| Verification subagent | `reviewer` correctness review | PASS | No code findings; noted plan artifacts were not yet finalized |
| Verification subagent | `cli-developer` first review | PASS after fix | Found empty cert/key edge; fixed and added regression tests |
| Verification subagent | `cli-developer` final review | PASS | Recommended `CLOSED` |

## Subagent summary
- Explorer mapped CLI, server, TLS manager, and tests; confirmed the baseline risks and recommended bounded validators plus constructor guards.
- Correctness reviewer verified non-empty acceptance paths and found no code defects.
- CLI reviewer found that empty `--cert`/`--key` values were treated as absent; the implementation was updated to reject empty values with exit code `2`.
- Final CLI verification confirmed all STAGE-004 acceptance criteria are met and recommended `CLOSED`.

## Files changed
- `src/cli.py`
- `src/server.py`
- `tests/test_cli.py`
- `implementation-plan/20260505-205639/stages/STAGE-004-cli-tls-and-limit-validation.md`
- `implementation-plan/20260505-205639/stage-status.md`
- `implementation-plan/20260505-205639/change-log.md`
- `implementation-plan/20260505-205639/stage-reports/STAGE-004-20260505-221247.md`

## Final status
CLOSED
