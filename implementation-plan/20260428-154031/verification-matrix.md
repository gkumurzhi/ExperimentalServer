# Verification Matrix

| Stage | Required checks | Optional checks | Known blockers | Baseline needed? |
|---|---|---|---|---|
| STAGE-001 | `pytest tests/test_handlers/test_path_traversal.py tests/test_http/test_path_traversal_prefix.py tests/test_server_routing.py -q`<br>`python -m compileall src tests` | Inspect resolver containment logic | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-002 | `pytest tests/test_server_methods.py tests/test_server_live.py -q`<br>`python -m compileall src tests` | Search for `read_bytes()` in response post-processing | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-003 | `pytest tests/test_server_methods.py -q -k smuggle`<br>`python -m compileall src tests` | Inspect SMUGGLE code path for full-size duplicate reads | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-004 | `pytest tests/test_websocket.py tests/test_websocket_handlers.py tests/test_security/test_websocket_frame_limit.py -q`<br>`python -m compileall src tests` | Inspect `_handle_notepad_ws()` parser call | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-005 | `pytest tests/test_server_routing.py tests/test_server_methods.py -q` plus relevant browser smoke if available<br>`python -m compileall src tests` | Compare `index.html` script tags to tracked/package data files | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-006 | `pytest tests -q -k "browser_smoke or ui or inspector"` or run the browser smoke target if available<br>`python -m compileall src tests` | Search for raw assignment/copy paths in UI files | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-007 | `docker build --no-cache --target runtime .` if Docker is available, otherwise static Dockerfile/COPY review<br>`docker buildx build --help >/dev/null` optional environment check | `git check-ignore -v notes .env example.key` using dummy names only | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-008 | `python -m pip_audit --help` and the updated audit command if `pip-audit` is installed<br>`python -m compileall src tests` | Review `security.yml` install/audit sequence | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-009 | `python3 tools/sync_docs.py --check`<br>`git diff --check API.md docs/api.md README.md` | Compare docs against `src/handlers/smuggle.py` | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-010 | `python3 tools/sync_docs.py --check`<br>`git diff --check API.md docs/api.md` | Compare documented error shapes to handler code | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-011 | `pytest tests/test_cli.py -q`<br>`git diff --check CLAUDE.md` | `rg "--opsec|--sandbox|Opsec|startswith" CLAUDE.md` | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-012 | `pytest tests/test_http tests/test_handlers/test_handler_integration.py tests/test_request_pipeline.py -q`<br>`python -m compileall src tests` | Inspect pipeline order | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-013 | `pytest tests/test_handlers/test_handler_integration.py tests/test_security/test_crypto.py -q`<br>`python -m compileall src tests` | Inspect `decrypt()` call sites | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-014 | `pytest tests/test_server_methods.py tests/test_handlers -q -k "hidden or smuggle or fetch or delete"`<br>`python -m compileall src tests` | Inspect all file method handlers | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-015 | `pytest tests/test_server_methods.py tests/test_http/test_response.py -q -k "cors or options"`<br>`python -m compileall src tests` | Inspect CORS headers for browser-valid values | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-016 | `pytest tests/test_websocket.py tests/test_websocket_handlers.py tests/test_security/test_websocket_upgrade.py tests/test_security/test_websocket_frame_limit.py -q`<br>`python -m compileall src tests` | Map parser outcomes to close codes | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-017 | `pytest tests/test_websocket.py tests/test_server_live.py tests/test_security/test_websocket_frame_limit.py -q`<br>`python -m compileall src tests` | Inspect worker/WS loop limits | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-018 | `pytest tests/test_request_pipeline.py tests/test_server_methods.py -q -k "metrics or error"`<br>`python -m compileall src tests` | Review all `metrics.record` call sites | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-019 | `pytest tests/test_http/test_content_length_smuggling.py tests/test_handlers/test_handler_integration.py -q`<br>`python -m compileall src tests` | Inspect receive and advanced-upload allocation paths | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-020 | `pre-commit run --all-files` if installed, otherwise `ruff check . && mypy src tests` using constrained env<br>`python -m compileall src tests` | Compare `.pre-commit-config.yaml` to `constraints/ci.txt` | Tool availability or existing dirty worktree may affect exact commands | recommended |
| STAGE-021 | `python -m pip install -c constraints/ci.txt -e .[crypto,lint,test]` or selected locked workflow command in a disposable env<br>`python -m compileall src tests` | Compare docs, CI, Docker, Dependabot, and lockfiles | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-022 | `docker compose -f examples/docker/docker-compose.yml config --quiet` if Docker Compose is available<br>`docker build .` if Docker is available | Review healthcheck and volume examples | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-023 | `node tools/browser_smoke.playwright.js` or project browser smoke wrapper if available<br>`python -m compileall src tests` | Inspect ARIA/status and capability handling | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-024 | `python3 tools/sync_docs.py --check`<br>`git diff --check README.md API.md SECURITY.md CHANGELOG.md CONTRIBUTING.md docs tools/sync_docs.py` | Map docs claims to source evidence listed in stage | Tool availability or existing dirty worktree may affect exact commands | yes |
| STAGE-025 | `git check-ignore -v notes/example.enc notes/example.meta.json` using dummy paths only<br>`python -m compileall src tests` | Review `.gitignore` runtime data section | Tool availability or existing dirty worktree may affect exact commands | recommended |
