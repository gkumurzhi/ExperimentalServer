# STAGE-008 Closure Report
_Generated: 2026-04-28 22:54:30 MSK_

## Stage target
- Stage: STAGE-008 - Repair pip-audit Security Workflow
- Requested by: `$close-plan-stage STAGE-008, запускай verification subagents автоматически`
- Active plan: `implementation-plan/20260428-154031`

## Baseline
- Branch: `main`
- Pre-existing changes: `?? notes/`
- Dependencies: satisfied; stage has no dependencies.

## Execution approach
1. Confirm `pip-audit` CLI semantics against the local workflow and official `pypa/pip-audit` documentation.
2. Update the security workflow to use a valid requirements/constraints audit command with `constraints/ci.txt` as the explicit dependency input.
3. Verify command parsing and behavior locally, run workflow/static checks, and launch verifier subagents for workflow semantics and dependency policy.

## Acceptance criteria from plan
- [x] Workflow no longer invokes an invalid or mismatched `pip-audit` mode.
- [x] The audit command has a clear dependency input.
- [x] Failure mode is a real vulnerability/configuration failure, not pre-audit command misuse.

## Documentation evidence
- Context7 `/pypa/pip-audit` docs: no-input `pip-audit` audits the current environment.
- Context7 `/pypa/pip-audit` docs: `--disable-pip` can only be used with hashed requirements files or `--no-deps`.
- Context7 `/pypa/pip-audit` docs: `-r/--requirement` audits a requirements file input.

## Changes made
- `.github/workflows/security.yml`: replaced `pip-audit --strict --disable-pip` with `python -m pip_audit --strict -r constraints/ci.txt`.
- `.github/workflows/security.yml`: made the audit tool installation use `python -m pip install pip-audit`, still constrained by the workflow-level `PIP_CONSTRAINT=constraints/ci.txt`.
- No dependency pins were changed. The updated audit command now surfaces existing vulnerability findings instead of suppressing or bypassing them.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| pip-audit help | `/tmp/exphttp-stage008-audit-venv-bTEnTO/bin/python -m pip_audit --help` | PASS | `pip-audit 2.10.0` installed and help parsed. |
| Updated audit command | `/tmp/exphttp-stage008-audit-venv-bTEnTO/bin/python -m pip_audit --strict -r constraints/ci.txt` | PASS_WITH_FINDINGS | Command parsed and reached real audit results; found known vulnerabilities in existing `constraints/ci.txt` pins: `cryptography==46.0.5`, `pip==24.0`, `Pygments==2.19.2`, and `pytest==9.0.2`. |
| Rejected installed editable mode | `python -m pip_audit --strict --local --skip-editable` after editable install in disposable venv | REJECTED | Failed before audit with `exphttp: distribution marked as editable`; not acceptable for this stage. |
| Rejected installed wheel mode | `python -m pip_audit --strict --local` after non-editable local install in disposable venv | REJECTED | Failed before audit with `exphttp: Dependency not found on PyPI`; not acceptable for this stage. |
| Python compile | `python -m compileall src tests` | PASS | No Python compile regressions from workflow-only change. |
| Workflow YAML parse | `python -c 'import yaml, pathlib; yaml.safe_load(pathlib.Path(".github/workflows/security.yml").read_text())'` | PASS | Workflow YAML parses. |
| Formatting | `git diff --check -- .github/workflows/security.yml implementation-plan/20260428-154031/stage-reports/STAGE-008-20260428-225430.md` | PASS | No whitespace errors. |
| Static workflow review | `rg -n "disable-pip|pip_audit|pip-audit|constraints/ci.txt" .github/workflows/security.yml constraints/ci.txt` | PASS | No `--disable-pip`; audit command uses `constraints/ci.txt` as explicit input. |

## Subagent verification
- Dependency verifier: PASS for the first installed-environment attempt; confirmed `--disable-pip` removal and constrained tool install. Later superseded by the constraints-input mode after local audit exposed editable/local-package collection failures.
- DevOps verifier: PASS for the first installed-environment attempt; confirmed failure mode was no longer `--disable-pip` misuse. Later superseded by the constraints-input mode after local audit exposed editable/local-package collection failures.
- Final DevOps verifier: PASS. Confirmed `python -m pip_audit --strict -r constraints/ci.txt` is a valid requirements-input audit, `.github/workflows/security.yml` contains no `--disable-pip`, and remaining live-CI risk is ordinary runner/network/vulnerability behavior rather than pre-audit command misuse.

## Final status
CLOSED - acceptance criteria met. The workflow now uses a documented requirements-input audit mode and any non-zero result comes from real vulnerability findings in existing pinned dependencies, not pre-audit command misuse.
