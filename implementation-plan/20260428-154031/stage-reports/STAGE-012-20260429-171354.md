# STAGE-012 Closure Report
_Generated: 2026-04-29 17:13:54 +0300_

## Stage target
- Stage: STAGE-012 — Reject Malformed Request Lines
- Requested by: `$close-plan-stage STAGE-012, запускай verification subagents автоматически`
- Active plan: `implementation-plan/20260428-154031`

## Baseline
- Branch: `main`
- Pre-existing changes: `?? notes/`
- Dependencies: satisfied; stage has no dependencies.

## Execution approach
1. Add parser validity state for malformed request lines while keeping well-formed custom methods supported.
2. Reject invalid parses in `RequestPipeline` with `400 Bad Request` before keep-alive, auth, payload-size checks, WebSocket handling, or dispatch.
3. Add request parser, pipeline, and advanced-upload regression coverage.
4. Run targeted tests, compile check, static diff review, and verification subagents.

## Acceptance criteria from plan
- [x] Malformed request lines cannot create files through advanced upload.
- [x] Invalid parse failures return 400 consistently.
- [x] Valid standard and documented custom methods still work.

## Implementation log
- Started by reading `src/http/request.py`, `src/request_pipeline.py`, `src/handlers/__init__.py`, targeted tests, active plan artifacts, and verification matrix.
- Spawned read-only explorer subagent to confirm code paths and regression-test shape.
- Added `HTTPRequest.parse_error` and `HTTPRequest.is_valid`, with request-line validation for exact method/target/version fields, HTTP method tokens, HTTP version tokens, and literal control/whitespace in request targets.
- Updated `RequestPipeline.process()` to send `400 Bad Request` immediately after parsing invalid requests, before keep-alive resolution, auth, WebSocket checks, payload-size checks, dispatch, post-processing, or upload side effects.
- Added a defense-in-depth validity guard in `HandlerMixin._dispatch_handler()` before method lookup and advanced-upload fallback.
- Added parser, pipeline, and handler integration regressions for malformed request lines, invalid request targets with advanced-upload payloads, and preserved valid unknown-method advanced-upload fallback.
- Normalized `src/http/request.py` and `tests/test_http/test_request.py` working-tree line endings to LF so `git diff --check` is clean for the new diff.
- First verifier found a blocking bypass for `XUPLOAD /\t HTTP/1.1`; fix-loop added request-target validation and dedicated regressions. Repeat verifier confirmed the bypass is closed.

## Files changed
- `src/http/request.py` — parser validity state and request-line validation.
- `src/request_pipeline.py` — early `400 Bad Request` for invalid parsed requests.
- `src/handlers/__init__.py` — defense-in-depth invalid-request guard before advanced-upload fallback.
- `tests/test_http/test_request.py` — parser invalidity regressions.
- `tests/test_request_pipeline.py` — invalid request `400` short-circuit regressions.
- `tests/test_handlers/test_handler_integration.py` — advanced-upload no-side-effect and valid fallback regressions.
- `implementation-plan/20260428-154031/stages/STAGE-012-malformed-request-rejection.md` — closure status and notes.
- `implementation-plan/20260428-154031/stage-status.md` — stage status row.
- `implementation-plan/20260428-154031/change-log.md` — closure entry.
- `implementation-plan/20260428-154031/stage-reports/STAGE-012-20260429-171354.md` — this report.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted tests | `.venv/bin/python -m pytest tests/test_http tests/test_handlers/test_handler_integration.py tests/test_request_pipeline.py -q` | PASS | `147 passed in 5.76s` after fix-loop. |
| Parser property tests | `.venv/bin/python -m pytest tests/test_property/test_request_parser.py -q` | PASS | `2 passed in 0.42s`; validates parser does not crash on fuzzed input. |
| Compile check | `python -m compileall src tests` | PASS | Compilation succeeded for source and tests. |
| Lint/static | `.venv/bin/python -m ruff check src/http/request.py src/request_pipeline.py src/handlers/__init__.py tests/test_http/test_request.py tests/test_request_pipeline.py tests/test_handlers/test_handler_integration.py` | PASS | `All checks passed!` |
| Diff whitespace | `git diff --check` | PASS | Clean after LF normalization of the two touched CRLF files. |
| Initial environment probe | `pytest tests/test_http tests/test_handlers/test_handler_integration.py tests/test_request_pipeline.py -q` | ENV_FAIL | Host/global Python lacks `pytest`; project `.venv` pytest passed. |
| Explorer subagent | `explorer` read-only review | PASS | Confirmed parser/pipeline/advanced-upload paths and regression shape. |
| Correctness verifier | `reviewer` read-only review | FAIL_THEN_PASS | First review found `XUPLOAD /\t HTTP/1.1` bypass; after fix-loop repeat review passed. |
| QA verifier | `qa-expert` read-only review | PASS | Recommended close; noted non-blocking extra parametrization opportunities. |
| Security verifier | `security-auditor` read-only review | PASS | Confirmed malformed request lines fail closed before auth/dispatch/upload on the production path. |

## Residual notes
- Percent-encoded controls such as `/%09` remain syntactically valid request targets and are decoded into `request.path`, matching existing URL-decoding behavior such as encoded spaces. Repeat verifier treated this as outside the literal malformed request-line bypass scope for STAGE-012.

## Stage result
CLOSED
