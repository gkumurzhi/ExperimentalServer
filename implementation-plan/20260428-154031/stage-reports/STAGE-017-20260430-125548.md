# STAGE-017 Closure Report
_Generated: 2026-04-30 12:55:48 MSK_

## Stage target
- Stage: STAGE-017 — Bound WebSocket Resource Use
- Requested by: `$close-plan-stage STAGE-017, запускай verification subagents автоматически`
- Active plan: `implementation-plan/20260428-154031`

## Baseline
- Branch: `main`
- Pre-existing changes: untracked `notes/` directory; not inspected or modified because it is runtime note data.
- Dependencies: satisfied; STAGE-016 is `CLOSED`.

## Execution approach
1. Map current WebSocket upgrade, frame parsing, worker lifecycle, and existing tests.
2. Add scoped admission/resource limits and bounded incomplete-frame handling using existing project conventions.
3. Add targeted tests for connection budget and incomplete-frame timeout behavior.
4. Run targeted tests, compile, static diff review, and an automatic verification subagent review.

## Acceptance criteria from plan
- [x] Idle WebSockets cannot occupy all request workers without an explicit configured budget.
- [x] Incomplete near-limit frames are closed after a bounded idle period.
- [x] Metrics/logging can distinguish rejected WS admissions if implemented.

## Implementation log
- Started from clean tracked tree with pre-existing untracked `notes/` runtime data.
- Added a WebSocket admission budget in `ExperimentalHTTPServer`, enforced before the blocking notepad WebSocket loop. The default budget is derived from worker count with `max(0, max_workers // 2)`, so single-worker servers preserve their only worker unless a caller explicitly configures a WebSocket budget.
- Wrapped admitted WebSocket handling in `try/finally` so slots are released after normal close and failures.
- Reworked `_handle_notepad_ws()` buffering from immutable `bytes` concatenation to `bytearray.extend()` plus consumed-prefix deletion.
- Added bounded incomplete-frame lifetime handling: partial frames switch the socket timeout to `websocket_frame_idle_timeout` and close with code `1002` if no completion arrives.
- Added WebSocket metrics for active connections and rejected admissions, plus warning logs for admission rejection and incomplete-frame timeout.
- Extended parser typing to accept `bytearray` input while preserving `bytes` payload return values.
- Added targeted tests for admission rejection, single-worker default budget, explicit override, normal HTTP availability while a WebSocket is held, incomplete-frame timeout, parser `bytearray` input, metrics counters, and pipeline acquire/release behavior.

## Files changed
- `src/server.py`
- `src/request_pipeline.py`
- `src/websocket.py`
- `src/metrics.py`
- `tests/test_server_live.py`
- `tests/test_server_methods.py`
- `tests/test_request_pipeline.py`
- `tests/test_websocket.py`
- `tests/test_websocket_handlers.py`
- `tests/test_metrics.py`

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline host pytest | `pytest tests/test_websocket.py tests/test_server_live.py tests/test_security/test_websocket_frame_limit.py -q` | FAIL | Host Python lacks `pytest`; project `.venv` verification was used. |
| Mandatory targeted tests | `.venv/bin/pytest tests/test_websocket.py tests/test_server_live.py tests/test_security/test_websocket_frame_limit.py -q` | PASS | `60 passed` after final single-worker budget fix. |
| Affected tests | `.venv/bin/pytest tests/test_server_methods.py tests/test_request_pipeline.py tests/test_websocket.py tests/test_server_live.py tests/test_security/test_websocket_frame_limit.py tests/test_websocket_handlers.py tests/test_metrics.py -q` | PASS | `160 passed`. |
| Compile | `.venv/bin/python -m compileall src tests` | PASS | Compilation completed. |
| Lint | `.venv/bin/ruff check src/server.py src/request_pipeline.py src/websocket.py src/metrics.py tests/test_server_methods.py tests/test_request_pipeline.py tests/test_websocket.py tests/test_server_live.py tests/test_websocket_handlers.py tests/test_metrics.py` | PASS | `All checks passed!` |
| Typecheck | `.venv/bin/mypy src` | PASS | `Success: no issues found in 33 source files`. |
| Whitespace/static diff | `git diff --check` | PASS | No whitespace errors. |
| Full suite | `timeout 120 .venv/bin/pytest -q` | PASS | `607 passed in 16.85s`. |
| Explorer subagent | `explorer` | PASS | Confirmed WebSocket admission, incomplete-frame, and metrics code paths before implementation. |
| Verification subagent | `reviewer` | PASS | Initial review found no blocking issues and identified the single-worker residual risk, which was fixed and reverified locally. |
| Final verification subagent | `reviewer` | PASS | No blocking findings after the single-worker default budget fix. |

## Outcome
- Status: CLOSED
- Closed at: 2026-04-30 13:25:36 MSK
- Notes: STAGE-017 acceptance criteria are met. Runtime `notes/` data remained uninspected and unmodified.
