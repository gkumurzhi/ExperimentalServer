# STAGE-021 Closure Report
_Generated: 2026-04-30 16:33:12 MSK_

## Stage target
- Stage: STAGE-021 — Set Dependency Authority and Update Coverage
- Requested by: `$close-plan-stage STAGE-021, run verification subagents automatically. Do not commit or push; the wrapper will do that.`
- Active plan: `implementation-plan/20260428-154031`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-020` is `CLOSED`)

## Execution approach
1. Confirm the active dependency authority across CI, Docker, local docs, lockfiles, and smoke tooling.
2. Apply a constraints-first policy because current CI, security, Docker, and local guidance already install with `constraints/ci.txt`.
3. Remove or ignore conflicting local lockfile authority, extend Dependabot to active automation surfaces, align Python support metadata with CI, and pin Playwright CLI resolution.
4. Run constrained install verification, compile/static checks, docs sync checks, workflow YAML parse/static review, and verification subagents.

## Acceptance criteria from plan
- [x] There is one clear dependency authority for CI/Docker/local guidance.
- [x] Dependabot covers the active dependency surfaces selected by the policy.
- [x] Python support metadata and CI matrix no longer disagree silently.
- [x] Browser smoke package resolution is pinned or documented as non-release-gating.

## Implementation log
- Chose constraints-first as the project dependency authority because CI, security, Docker, and existing local guidance already used `constraints/ci.txt`.
- Removed the tracked `uv.lock` and ignored future local `uv.lock` files so there is no second committed Python authority.
- Documented `constraints/ci.txt` as the only committed dependency authority in root and generated docs.
- Added Dependabot coverage for Docker and pre-commit alongside existing pip and GitHub Actions surfaces.
- Made Docker base-image pins directly visible in `FROM` lines so Docker Dependabot can inspect the active base image.
- Capped package metadata at `>=3.10,<3.14` and removed stale open-ended Python support wording.
- Pinned browser-smoke Playwright resolution to `@playwright/cli@0.1.9` in both CI browser installation and the smoke helper fallback, and documented that the release-gating pins must move together.

## Files changed
- `.github/dependabot.yml`
- `.github/workflows/ci.yml`
- `.gitignore`
- `CHANGELOG.md`
- `CLAUDE.md`
- `CONTRIBUTING.md`
- `Dockerfile`
- `README.md`
- `docs/ADR/ADR-003-cryptography-optional.md`
- `docs/changelog.md`
- `docs/contributing.md`
- `docs/index.md`
- `pyproject.toml`
- `tools/browser_smoke.py`
- `uv.lock` (deleted)

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted constrained install | `python -m venv /tmp/stage021-install-*/venv && /tmp/stage021-install-*/venv/bin/python -m pip install -c "$PWD/constraints/ci.txt" -e ".[crypto,lint,test]"` | PASS | Installed with constrained pins and import check passed. |
| Compile | `python -m compileall src tests tools/browser_smoke.py` | PASS | Source, tests, and smoke wrapper compiled. |
| Lint | `ruff check src tests tools` | PASS | No Ruff findings. |
| Format | `ruff format --check src tests tools` | PASS | 74 files already formatted. |
| Type check | `tmp venv + python -m mypy src` | PASS | Constrained disposable venv reported no issues in 33 source files. Host `mypy` entry point failed before running due to a missing global module and was treated as an environment issue. |
| Docs sync | `python tools/sync_docs.py --check` | PASS | Generated docs mirrors are in sync. |
| YAML parse | Python `yaml.safe_load` for `.github/dependabot.yml`, `.github/workflows/ci.yml`, `.github/workflows/security.yml` | PASS | Workflow and Dependabot YAML parsed. |
| Metadata parse | Python `tomllib` check for `requires-python` | PASS | `pyproject.toml` is `>=3.10,<3.14`. |
| Local lock ignore | `git check-ignore -v --no-index uv.lock` | PASS | `.gitignore` ignores future local `uv.lock` files. |
| Playwright command | `npx --yes --package @playwright/cli@0.1.9 playwright-cli --help` | PASS | Pinned CLI resolves. |
| Browser install dry run | `npx --yes --package @playwright/cli@0.1.9 playwright-cli install-browser chromium --with-deps --dry-run` | PASS | Dry run lists the pinned CLI's Chromium/browser assets. |
| Browser smoke fallback | Python check of `tools.browser_smoke._playwright_command()` with empty `CODEX_HOME` | PASS | Fallback command includes `@playwright/cli@0.1.9`. |
| Docker build | `docker build --target runtime -t exphttp:stage021 .` | PASS | Runtime image built with direct pinned `FROM` references and constrained wheel install. |
| Docker smoke | `docker run --rm exphttp:stage021 --version` | PASS | Printed `exphttp 2.0.0`. |
| Static policy review | Greps for stale Python support, unpinned Playwright commands, and Docker ARG indirection | PASS | No stale `Python 3.10+`, unpinned release-smoke Playwright, or `${PYTHON_BASE_IMAGE}` references remain. |
| Diff hygiene | `git diff --check` | PASS | No whitespace errors. |

## Subagent verification
- `devops-engineer` read-only explorer identified the constraints-first baseline, `uv.lock` conflict, missing Docker/pre-commit Dependabot surfaces, Python metadata gap, and unpinned browser smoke path.
- `dependency-manager` read-only explorer confirmed the same policy direction and requested public docs cleanup for open-ended Python support.
- First post-change dependency verifier failed on Docker Dependabot visibility because the base image pin was hidden behind an `ARG`; fixed by using direct pinned `FROM` lines.
- First reviewer failed on mismatched Playwright pins between CI browser install and smoke runner; fixed by using the same `@playwright/cli@0.1.9` package in both places.
- Final reviewer passed all four acceptance criteria with no findings.

## Outcome
- Status: CLOSED
- Completed: 2026-04-30 17:02:04 MSK
