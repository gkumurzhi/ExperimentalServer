# STAGE-007 Closure Report
_Generated: 2026-05-21T23:58:01+03:00_

## Stage target
- Stage: STAGE-007 - Harden UI validation and CSP release gates
- Requested by: `$close-plan-stage STAGE-007 --from implementation-plan/20260521-212008/stages/STAGE-007-ui-csp-release-gates.md --with-subagents`
- Active plan: `implementation-plan/20260521-212008`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-005` is `CLOSED`)

## Execution approach
1. Inspect static UI packaging, current CSP headers/tests, browser smoke error handling, and CI/pre-commit release gates.
2. Add fast JavaScript/package-data validation and wire it into local/CI gates.
3. Make browser smoke fail on page errors and unexpected console errors without broad UI changes.
4. Add CSP tests and tighten CSP where current assets prove inline script/style allowances are unnecessary.
5. Run targeted verification, update active plan artifacts, and record outcomes here.

## Acceptance criteria from plan
- [x] Broken bundled JavaScript fails a fast local/CI check.
- [x] Package-data UI assets are validated outside source-tree-only assumptions.
- [x] Browser smoke captures page errors and unexpected console errors.
- [x] CSP behavior is tested and documented; any remaining inline allowance has an explicit reason.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| JS/package source gate | `python tools/check_static_ui_assets.py` | PASS | Validates source assets, importlib resources, package-data patterns, and `node --check` over bundled JS. |
| Browser smoke syntax | `node --check tools/browser_smoke.playwright.js` | PASS | Smoke script parses after placeholder additions. |
| CSP/static targeted tests | `pytest -q tests/test_handlers/test_handler_integration.py -k 'csp or static' tests/test_server_routing.py -k 'csp or static'` | PASS | 10 selected tests passed. |
| Broader handler/routing tests | `pytest -q tests/test_handlers/test_handler_integration.py tests/test_server_routing.py` | PASS | 122 tests passed. |
| Lint | `ruff check tools/browser_smoke.py tools/check_static_ui_assets.py tests/test_handlers/test_handler_integration.py tests/test_server_routing.py src/handlers/files.py` | PASS | Touched Python paths passed. |
| Format | `ruff format --check tools/browser_smoke.py tools/check_static_ui_assets.py tests/test_handlers/test_handler_integration.py tests/test_server_routing.py src/handlers/files.py` | PASS | 5 files already formatted. |
| Python compile | `python -m py_compile tools/check_static_ui_assets.py tools/browser_smoke.py` | PASS | Tool syntax/import bootstrap checks passed. |
| Wheel package-data smoke | `wheel_dir=$(mktemp -d); python -m pip wheel . --no-deps --wheel-dir "${wheel_dir}"; python tools/check_static_ui_assets.py --wheel "${wheel_dir}"/exphttp-*.whl` | PASS | Built `exphttp-2.0.0` wheel and validated packaged UI assets. |
| Browser smoke | `python tools/browser_smoke.py` | PASS | Full happy/unavailable browser flows passed under CSP with pageerror/console capture. |
| Pre-commit UI hook | `.venv/bin/pre-commit run static-ui-release-gates --all-files` | PASS | `pre-commit` was not on PATH; project virtualenv hook passed. |
| Pre-commit smoke syntax hook | `.venv/bin/pre-commit run browser-smoke-js-syntax --all-files` | PASS | Project virtualenv hook passed. |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors. |

## Subagent review
- `security-auditor` reviewed the scoped CSP/UI gate surface read-only. Initial findings called out missing browser error capture, missing fast JS/package-data gates, the broken direct browser smoke import path, and undocumented inline CSP allowance.
- Resolution: browser smoke now bootstraps repo imports and installs page/console capture before navigation; CI/pre-commit run the static UI gates; inline scripts are blocked; the remaining inline style allowance is explicit in code and asserted by tests.

## Files changed
- `.github/workflows/ci.yml` - adds release-smoke static UI wheel/JS gates.
- `.pre-commit-config.yaml` - adds local static UI and browser-smoke syntax hooks.
- `src/data/static/ui/requests.js` - keeps OPTIONS preflight header in preview only, avoiding browser forbidden-header console errors.
- `src/handlers/files.py` - centralizes and tightens HTML CSP.
- `tests/test_handlers/test_handler_integration.py` - adds precise CSP/static header assertions.
- `tests/test_server_routing.py` - expands static package-data coverage to stylesheets and asserts CSP directives.
- `tools/browser_smoke.playwright.js` - captures page errors and unexpected console errors.
- `tools/browser_smoke.py` - makes direct script invocation import-safe and installs capture before first navigation.
- `tools/check_static_ui_assets.py` - adds source/importlib/wheel package-data and JS syntax gate.

## Outcome
Status: CLOSED
