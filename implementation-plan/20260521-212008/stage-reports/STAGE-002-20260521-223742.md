# STAGE-002 Closure Report
_Generated: 2026-05-21T22:37:42+03:00_

## Stage target
- Stage: STAGE-002 - Add receive-layer header caps and parser telemetry
- Requested by: `$close-plan-stage STAGE-002 --from implementation-plan/20260521-212008/stages/STAGE-002-receive-header-caps.md --with-subagents`
- Active plan: `implementation-plan/20260521-212008`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; `STAGE-001` is `CLOSED`
- Previous reports for this stage: none in the active plan directory

## Execution approach
1. Trace the current receive/parser code paths and tests for request framing, upload caps, and metrics.
2. Add focused tests proving headers over the configured cap fail before body buffering and that header/body caps are independent.
3. Implement the smallest parser/server/config/metrics plumbing required for header cap rejections and observable receive reasons.
4. Run the plan's targeted parser and server-helper verification, then perform a static diff review.

## Acceptance criteria from plan
- [x] Headers over the cap are rejected before body buffering.
- [x] Body upload cap and header cap are independent.
- [x] Existing duplicate/conflicting `Content-Length` tests still pass.
- [x] Receive rejection reasons can be observed or tested.

## Subagent log
- `explorer`: traced `_handle_client` -> `_receive_request` -> `src.http.io.receive_request`, confirmed there was no independent header cap or receive rejection telemetry, identified parser test locations, and warned about `RECV_CHUNK` overshoot and same-chunk body bytes.
- `reviewer`: initially found that the post-header size guard still compared total request bytes, which coupled max-size headers with max-size bodies. After the fix, the reviewer re-checked `src/http/io.py` and `tests/test_http/test_io.py`, ran the parser/smuggling tests, and recommended closure.

## Changes made
- `src/http/io.py`: added a default 64 KiB header-section cap, limited pre-header `recv` sizing to the configured header probe window, preserved existing `Content-Length` smuggling behavior, and added optional receive rejection callbacks for header/body/timeout/framing reasons.
- `src/server.py`: added `max_header_size` constructor validation/configuration, passed it into receive I/O, and recorded receive rejection reasons into metrics.
- `src/metrics.py`: added thread-safe `receive_rejections` counters to metrics snapshots.
- `src/cli.py`: added operator flag `--max-header-size KB` with default `64`.
- `tests/test_http/test_io.py`: added header-cap boundary, no-second-body-read, max-header plus max-body boundary, cap independence, and rejection-reason tests.
- `tests/test_metrics.py`, `tests/test_server_methods.py`, `tests/test_cli.py`: covered metrics counters, server receive telemetry, constructor/CLI validation, and startup output.
- `README.md`, `docs/api.md`, `docs/threat-model.md`: documented the new header cap flag and observable receive rejection metrics.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Baseline parser tests | `pytest -q tests/test_http/test_io.py tests/test_http/test_content_length_smuggling.py` | PASS | Pre-change baseline: 24 passed. |
| Baseline server helper tests | `pytest -q tests/test_server_methods.py -k 'payload_size or metrics or keep_alive'` | PASS | Pre-change baseline: 4 passed, 49 deselected. |
| Subagent exploration | `explorer` subagent | PASS | Read-only trace found the expected receive-path gap and no overlapping changes. |
| Parser tests | `pytest -q tests/test_http/test_io.py tests/test_http/test_content_length_smuggling.py` | PASS | 29 passed. Covers header cap, max-header plus max-body independence, and existing `Content-Length` smuggling regressions. |
| Server helper tests | `pytest -q tests/test_server_methods.py -k 'payload_size or metrics or keep_alive'` | PASS | 5 passed, 49 deselected. |
| Metrics and CLI tests | `pytest -q tests/test_metrics.py tests/test_cli.py` | PASS | 79 passed. |
| Metrics/CLI/docs/integration bundle | `pytest -q tests/test_metrics.py tests/test_cli.py tests/test_check_stale_docs.py tests/test_handlers/test_handler_integration.py -k 'metrics or not metrics'` | PASS | 182 passed after the final parser fix. |
| Docs stale checks | `pytest -q tests/test_check_stale_docs.py` | PASS | 6 passed. |
| Metrics endpoint integration | `pytest -q tests/test_handlers/test_handler_integration.py -k metrics` | PASS | 2 passed, 95 deselected. |
| Lint | `ruff check src/http/io.py src/server.py src/metrics.py src/cli.py tests/test_http/test_io.py tests/test_metrics.py tests/test_server_methods.py tests/test_cli.py` | PASS | All checks passed. |
| Compile | `python -m compileall -q src` | PASS | No syntax/compile errors. |
| Diff whitespace | `git diff --check` | PASS | No whitespace errors. |
| Verification subagent | `reviewer` subagent review and re-check | PASS | Initial high finding was fixed by measuring body bytes separately from header bytes; re-check ran parser/smuggling tests and recommended closure. |
| Type check | `python -m mypy src/http/io.py src/server.py src/metrics.py src/cli.py` | FAIL (environment) | `No module named mypy`; `importlib.util.find_spec('mypy')` returned `None`. Not caused by this stage. |
| Full suite | `pytest -q` | FAIL (environment) | Collection failed because `hypothesis` is not installed for property tests. Not caused by this stage. |
| Non-property suite | `pytest -q --ignore=tests/test_property` | FAIL (unrelated environment/dependency) | 667 passed; 9 TLS tests failed because `acme` is not installed and local `cryptography` 41.0.7 lacks newer `not_valid_*_utc` certificate attributes. Touched files are unrelated to TLS. |

## Result
Status: CLOSED

All stage acceptance criteria are met by targeted tests and static checks. Remaining suite/typecheck failures are environment or TLS dependency issues outside STAGE-002.
