# Findings

| Finding ID | Source | Severity | Area | Evidence | Root cause | Proposed outcome | Confidence |
|---|---|---:|---|---|---|---|---:|
| F-001 | `project-analysis-report.md`, `security-auditor.md`, `architect-reviewer.md`, `performance-engineer.md`, `qa-expert.md`, `docker-expert.md`, `python-pro.md`, `reviewer.md` | HIGH | Storage safety | `--max-size` is per request; uploads, notes, and SMUGGLE artifacts accumulate without total byte/count/retention enforcement | No shared storage policy or pre-write reservation boundary | Add storage policy with quotas, retention, reserved free space, pre-write rejection, tests, and docs correction | High |
| F-002 | `performance-engineer.md`, `python-pro.md`, `reviewer.md` | HIGH | Memory safety | `receive_request()` buffers chunks and `HTTPRequest.body` holds full upload; defaults imply about `max_workers * max_size` body pressure | Request body receive path has no process-wide memory budget or streaming upload path | Add declared-length reservation or streaming temp-file receive path with budget tests | High |
| F-003 | `performance-engineer.md`, `reviewer.md` | HIGH | Worker availability | Incomplete bodies can occupy workers through header plus body timeout, about 330 seconds by default | Body receive has broad total timeout but no idle/read-rate/deadline admission control | Add configurable body idle/deadline/read-rate protection and slow-body metrics | High |
| F-004 | `security-auditor.md`, `devops-engineer.md`, `docker-expert.md`, `qa-expert.md`, `reviewer.md` | HIGH | Auth secrets | CLI/service examples only support inline `--auth user:password`; Compose shows inline credential examples | No non-argv secret source for service/container deployments | Add `--auth-file` with precedence/failure/redaction behavior and update docs/Compose/tests | High |
| F-005 | `project-analysis-report.md`, `architect-reviewer.md`, `security-auditor.md`, `qa-expert.md`, `reviewer.md` | HIGH | Feature profiles | `DELETE`, `NOTE`, `SMUGGLE`, advanced upload, and clear operations are always behind one global auth decision | No capability model or registry/profile single source of truth | Add `FeatureSet`/`ServerProfile` and derive registry, CORS methods, PING, WebSocket, UI affordances, and tests from it | High |
| F-006 | `security-auditor.md`, `qa-expert.md`, `reviewer.md` | MEDIUM | CORS/browser trust | Wildcard CORS currently allows browser mutations and WebSocket origins; tests assert this behavior | Wildcard read CORS is reused as write/WS trust signal | Make `*` read-only; require exact origins for mutations and WebSocket upgrades | High |
| F-007 | `docker-expert.md`, `devops-engineer.md`, `qa-expert.md`, `security-auditor.md`, `reviewer.md` | MEDIUM | Docker health | Dockerfile probes unauthenticated plain HTTP PING; auth/TLS/ACME modes need overrides or disable health | Healthcheck is not mode-aware | Keep default plain health, add auth/TLS override examples and CI validation | High |
| F-008 | `docker-expert.md`, `reviewer.md` | MEDIUM | Docker exposure/resources | Compose publishes `"8080:8080"` and has no mem/cpu/pid/ulimit/volume quota examples | Example deployment lacks safe operational envelope | Bind default Compose to loopback, add resource/stop-grace examples and volume quota guidance | High |
| F-009 | `devops-engineer.md`, `docker-expert.md`, `project-analysis-report.md` | MEDIUM | Release supply chain | No release/publish workflow, artifact-of-record, SBOM/provenance/attestation/signing lane | Merge CI exists but release path is manual/implicit | Add tag-gated artifact workflow with wheel/sdist, optional container digest, SBOM/provenance, and scanning gates | High |
| F-010 | `devops-engineer.md`, `qa-expert.md` | MEDIUM | Release smoke | Browser smoke imports from source tree; Docker smoke is PING-focused and not exact installed UI runtime | Smoke tests do not exercise the built wheel/container artifact users receive | Add isolated installed-wheel smoke and container UI/runtime smoke | High |
| F-011 | `architect-reviewer.md`, `python-pro.md`, `qa-expert.md`, `project-analysis-report.md` | MEDIUM | Package identity | Distribution is `exphttp`, but public import/console path is `src`; docs show `from src` and `python -m src` | Current layout makes generic `src` the public import package | Add package discovery guard, migrate public import package to `exphttp`, and keep compatibility shim if needed | High |
| F-012 | `architect-reviewer.md` | MEDIUM | Auth invariants | README shows assigning `server.authenticator` after construction, but rate limiter is initialized only in `__init__` | Auth mutation bypasses server invariant setup | Replace with constructor injection or `set_authenticator()` that refreshes limiter state | High |
| F-013 | `python-pro.md` | MEDIUM | Storage atomicity | `write_unique_file_exclusive()` writes final path directly; concurrent readers or crashes can observe partial files | Exclusive creation prevents clobbering but not atomic publish | Write hidden temp file and atomically rename final file; clean stale temps | High |
| F-014 | `performance-engineer.md` | MEDIUM | Listing scalability | INFO and Notepad list paths scan/sort broad state before applying response limits | Pagination limits response size after full server-side work | Bound iteration/pagination and add tests for large directory/note lists | High |
| F-015 | `performance-engineer.md` | MEDIUM | Slow downloads | Streamed response uses blocking `sendall()` without total transfer deadline | Response timeout is per blocked socket operation, not whole transfer | Add response send deadline/abort metric and consider `sendfile()` follow-up | Medium |
| F-016 | `performance-engineer.md` | LOW | WebSocket operations | Constructor has WebSocket capacity/timeouts; CLI does not expose them | Runtime knobs are not operator-configurable | Add CLI flags and tests for WebSocket limits | High |
| F-017 | `python-pro.md` | MEDIUM | WebSocket failure semantics | Unexpected WebSocket exceptions log at debug and close with normal code `1000` | WS internal errors are not surfaced as failures | Log exceptions, increment metric, close with `1011` or explicit error frame | High |
| F-018 | `security-auditor.md` | MEDIUM | Reverse proxy auth rate limiting | In-memory limiter keys on backend client address and ignores proxy headers | No trusted proxy model | Decide proxy model; document proxy-level rate limiting or implement strict trusted CIDR | High |
| F-019 | `security-auditor.md`, `qa-expert.md` | LOW | Notepad product semantics | Docs say session-bound keys are not durably recoverable; metadata remains plaintext | Product expectation between scratchpad and durable notes is unresolved | Decide Notepad contract before durable recovery or per-user authorization work | Medium |
| F-020 | `devops-engineer.md`, `qa-expert.md` | LOW | Coverage/process | CI fail-under is global 65 and local command differs | Coverage gate is not risk-aligned | Move local/CI gate to shared config and add module-risk gates after high-risk stages | High |
| F-021 | `devops-engineer.md` | LOW | Toolchain drift | Playwright and hook/tool pins are duplicated outside constraints | Constraint authority is incomplete | Add parity check after release workflow shape is stable | Medium |
| F-022 | `qa-expert.md`, `security-auditor.md` | MEDIUM | ACME lifecycle | TLS/ACME setup and renewal decisions run at startup; long-running reload is not implemented | Operational renewal model is implicit | Document restart-before-expiry or implement tested renewal/reload in a later stage | Medium |
| F-023 | `python-pro.md` | LOW | Notepad clear contract | Note clear can partially delete then return generic failure | Partial failure reporting differs from upload clear | Return deleted counts/errors like upload clear if Notepad API is promoted | High |

