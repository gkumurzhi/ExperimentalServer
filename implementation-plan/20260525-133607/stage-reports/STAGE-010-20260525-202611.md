# STAGE-010 Closure Report
_Generated: 2026-05-25 20:26:11 MSK_

## Stage target
- Stage: STAGE-010 - Public package identity migration
- Requested by: `$close-plan-stage STAGE-010 --from implementation-plan/20260525-133607/stages/STAGE-010-public-package-identity.md --with-subagents`
- Active plan: `implementation-plan/20260525-133607`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; STAGE-009 is CLOSED

## Execution approach
1. Inventory current packaging, import entry points, public docs, tools, and tests that reference the generic `src` package.
2. Introduce `exphttp` as the public import and module entry point while preserving a compatibility path for existing `src` imports.
3. Harden package discovery so static asset directories are not treated as packages.
4. Update import-boundary, CLI, stale-doc, and installed-wheel smoke coverage.
5. Run targeted tests, package smoke checks, stale-doc guard, lint/format/type checks, and final diff review.

## Acceptance criteria from plan
- [x] `import exphttp` is the documented public import path and is tested.
- [x] `exphttp --help` and `python -m exphttp --help` work from an installed wheel.
- [x] Package discovery does not treat static asset directories as import packages.
- [x] Existing `src` compatibility is preserved with warnings/tests.
- [x] Docs and examples no longer promote `from src` or `python -m src` as the primary API.

## Subagents
- Nested `close-plan-stage --with-subagents` performed a focused package-identity pass before stalling while waiting for its final verifier subagent. The final closure decision is based on direct review plus the verification commands below.

## Files changed
- `exphttp/` - new public package adapter, module entry point, CLI/config/server exports, security/crypto exports, and `py.typed` marker.
- `src/__init__.py`, `src/__main__.py`, `src/cli.py` - legacy public API warning, compatibility entry-point wording, and internal imports that avoid self-triggering the new warning.
- `pyproject.toml` - console script target moved to `exphttp.cli:main`; package discovery now includes `exphttp*` and `src*` with `namespaces = false`; explicit package data preserves typed marker and static assets.
- `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `.github/workflows/security.yml` - public CLI/import smoke commands updated to `exphttp`.
- `README.md`, `CLAUDE.md`, `docs/architecture.md`, `docs/contributing.md` - public command/import documentation updated to `exphttp` while describing `src` as compatibility.
- `tools/browser_smoke.py`, `tools/decrypt.py`, `tools/check_stale_docs.py` - local tooling now uses/guards the public `exphttp` identity.
- `tests/test_import_boundaries.py`, `tests/test_cli.py`, `tests/test_check_stale_docs.py` - import, CLI, package-discovery, compatibility warning, and stale-doc regression coverage.
- Plan artifacts for STAGE-010.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
- Docs guard | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
- Lint | `ruff check exphttp src tests tools` | PASS | All checks passed. |
- Format check | `ruff format --check exphttp src tests tools/browser_smoke.py tools/check_stale_docs.py tools/decrypt.py` | PASS | Focused on changed package/test/tool paths. A broader `ruff format --check ... tools` still reports ignored, untracked `tools/close_plan_stages.py` as unrelated pre-existing/generated local state. |
- Type check | `/tmp/exphttp-lint-env/bin/mypy exphttp src` | PASS | Temporary lint venv used because the active Python environment's direct `mypy` launcher is not reliable. |
- Targeted tests | `python -m pytest tests/test_import_boundaries.py tests/test_cli.py tests/test_pytest_collection_policy.py tests/test_check_stale_docs.py` | PASS | 130 tests passed. |
- Isolated wheel build | `cd /tmp && /tmp/exphttp-release-tools/bin/python -m build --wheel --outdir /tmp/exphttp-stage010-dist /home/user/PycharmProjects/ExperimentalHTTPServer` | PASS | Built `exphttp-2.0.0-py3-none-any.whl` without loading the checkout-local ignored `build/` directory as the build frontend. |
- Installed wheel CLI/import smoke | Fresh `/tmp/exphttp-stage010-smoke` venv; installed `/tmp/exphttp-stage010-dist/exphttp-*.whl`; ran `exphttp --help`, `python -m exphttp --help`, `import exphttp`, `from exphttp import ExperimentalHTTPServer`, `from src import ExperimentalHTTPServer` warning probe | PASS | Public and legacy imports resolved under `/tmp/exphttp-stage010-smoke/lib/python3.12/site-packages`, not the checkout. |
- Static UI wheel asset check | `python tools/check_static_ui_assets.py --wheel-only --wheel '/tmp/exphttp-stage010-dist/exphttp-*.whl'` | PASS | Wheel contains required UI assets. |
- Installed-package browser smoke | `GITHUB_WORKSPACE=/home/user/PycharmProjects/ExperimentalHTTPServer /tmp/exphttp-stage010-smoke/bin/python /home/user/PycharmProjects/ExperimentalHTTPServer/tools/browser_smoke.py --installed-package` | PASS | Browser smoke passed; server module resolved from the installed wheel venv. |
- Diff hygiene | `git diff --check` | PASS | No whitespace errors. |

## Outcome
Status: CLOSED

STAGE-010 now exposes `exphttp` as the public import and module identity while
preserving `src` as a tested compatibility path with a deprecation warning for
public lazy exports. Packaging discovery is explicit and namespace-safe, static
assets remain packaged, docs/CI/tooling no longer promote `python -m src` or
`from src`, and installed-wheel smoke proves `exphttp` works outside the source
checkout.
