# STAGE-009 Closure Report
_Generated: 2026-05-25 20:00:07 MSK_

## Stage target
- Stage: STAGE-009 - Release artifacts and provenance lane
- Requested by: `$close-plan-stage STAGE-009 --from implementation-plan/20260525-133607/stages/STAGE-009-release-artifacts-provenance.md --with-subagents`
- Active plan: `implementation-plan/20260525-133607`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-008` is `CLOSED`)

## Execution approach
1. Inventory current packaging metadata, CI/security workflows, static UI validation, installed-artifact smoke capabilities, and release documentation.
2. Add a tag/manual release workflow that builds wheel/sdist, performs installed-wheel checks outside the source tree, validates static assets from the built wheel, uploads artifacts, and includes explicit provenance/SBOM hooks without publishing by default.
3. Add or adjust narrow tooling so local and CI release checks can exercise the built wheel without importing from the checkout.
4. Document artifact promotion, provenance/SBOM evidence, and rollback by package version or image digest.
5. Run the stage verification commands, review the final diff, and update plan artifacts.

## Acceptance criteria from plan
- [x] Release workflow builds wheel and sdist as artifacts of record.
- [x] Installed-wheel smoke runs without importing from the source tree.
- [x] Static UI assets are checked from the built wheel.
- [x] Workflow permissions are minimal and explicit.
- [x] SBOM/provenance/attestation/scanning path is present or explicitly documented as blocked by publish intent.

## Subagents
- `explorer` (`019e6015-04a0-7481-ab55-48fdb5c80c24`): read-only inventory of release/build workflows, packaging, static UI, browser smoke, and release docs. Found no existing release workflow, identified source-tree import risk in `tools/browser_smoke.py`, and recommended wheel-only/static and installed-package smoke support.
- `reviewer` (`019e6024-5afc-7a91-9dc9-3fe10bf04514`): read-only review of the final release-lane changes. Found no blocking issues and confirmed the stage is closeable.

## Files changed
- `.github/workflows/release.yml` - new tag/manual release workflow that builds wheel/sdist, runs installed-wheel CLI/import/static/browser smoke, scans pinned dependencies, emits a CycloneDX SBOM, attests artifacts, and uploads artifacts of record.
- `.github/workflows/ci.yml` - explicit `contents: read` workflow permissions.
- `.github/workflows/security.yml` - explicit `contents: read` workflow permissions.
- `tools/browser_smoke.py` - installed-package mode that removes checkout import paths and fails if the server module resolves under the repo.
- `tools/check_static_ui_assets.py` - `--wheel-only` mode to validate built wheel assets without importing the source tree.
- `constraints/ci.txt` - refreshed `idna`, `pymdown-extensions`, and `urllib3` pins after the release scan exposed current advisories.
- `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `docs/contributing.md`, `docs/changelog.md` - release artifact, promotion, rollback, SBOM/provenance, and changelog documentation.
- `docs/api.md`, `docs/security.md` - generated MkDocs mirrors resynced from existing root-canonical docs while running the repository docs sync tool.
- `implementation-plan/20260525-133607/stages/STAGE-009-release-artifacts-provenance.md`, `implementation-plan/20260525-133607/stage-status.md`, `implementation-plan/20260525-133607/change-log.md` - plan closure artifacts.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
- Documentation lookup | Context7 `/websites/github_en_actions` and `/pypa/pip-audit` queries | PASS | Confirmed GitHub artifact attestation permissions/inputs and pip-audit CycloneDX JSON flags from current docs. |
- Local tool setup probe | `python -m build --version` | FAIL | Local ignored `build/` directory shadows the PyPI `build` module; release workflow now removes generated `build/` before running `python -m build`. |
- Local tool setup probe | `python -m pip install --user -c constraints/ci.txt build pip-audit` | FAIL | System Python is PEP 668 externally managed; installed release tools into `/tmp/exphttp-release-tools` instead. |
- Python syntax | `python -m py_compile tools/browser_smoke.py tools/check_static_ui_assets.py` | PASS | Touched tools compile. |
- Static UI source check | `python tools/check_static_ui_assets.py --skip-node-check` | PASS | Existing source-tree mode still works. |
- Wheel-only guard | `python tools/check_static_ui_assets.py --wheel-only` | PASS | Expected failure path returns non-zero with actionable error when no wheel is provided. |
- YAML syntax | Python `yaml.safe_load` over `.github/workflows/*.yml` | PASS | CI, release, and security workflows parse. |
- Lint | `ruff check tools/browser_smoke.py tools/check_static_ui_assets.py tools/sync_docs.py` | PASS | Touched tooling passes ruff. |
- Docs sync | `python tools/sync_docs.py --check` | PASS | Generated docs mirrors are in sync. |
- Docs stale guard | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
- Local build | `PATH="/tmp/exphttp-release-tools/bin:${PATH}" python -m build --sdist --wheel --outdir /tmp/exphttp-dist` | PASS | Produced `exphttp-2.0.0-py3-none-any.whl` and `exphttp-2.0.0.tar.gz`; local ignored `build/` was temporarily moved and restored. |
- Isolated wheel smoke | Fresh `/tmp/exphttp-wheel-smoke-final` venv, install `/tmp/exphttp-dist/exphttp-*.whl`, run `exphttp --version`, `exphttp --help`, and import path probe | PASS | CLI ran and imports resolved under `/tmp/exphttp-wheel-smoke-final/lib/python3.12/site-packages`, not the checkout. |
- Static UI artifact check | `python tools/check_static_ui_assets.py --wheel-only --wheel '/tmp/exphttp-dist/exphttp-*.whl'` | PASS | Built wheel contains required UI assets. |
- Installed wheel browser smoke | `GITHUB_WORKSPACE=/home/user/PycharmProjects/ExperimentalHTTPServer /tmp/exphttp-wheel-smoke-final/bin/python /home/user/PycharmProjects/ExperimentalHTTPServer/tools/browser_smoke.py --installed-package` | PASS | Full browser smoke passed; result reported `serverModulePath` in the wheel venv site-packages. |
- Dependency scan/SBOM | `/tmp/exphttp-release-tools/bin/python -m pip_audit --strict --no-deps -r constraints/ci.txt`; then CycloneDX JSON output to `/tmp/exphttp-dist/exphttp-dependency-sbom.cdx.json` | PASS | Initial scan failed on `idna`, `pymdown-extensions`, and `urllib3`; after pin refresh, scan and SBOM generation passed. |
- Targeted regression tests | `python -m pytest tests/test_check_stale_docs.py tests/test_import_boundaries.py tests/test_cli.py` | PASS | 123 tests passed. |
- Installed wheel dependency check | `/tmp/exphttp-wheel-smoke/bin/python -m pip check` | PASS | No broken requirements found in isolated wheel smoke venv. |
- Diff hygiene | `git diff --check` | PASS | No whitespace errors. |
- Verification subagent | `reviewer` read-only review | PASS | No blockers; stage closeable. |

## Outcome
Status: CLOSED

STAGE-009 now has a controlled artifact-only release lane. The workflow builds
wheel and sdist, validates the installed wheel without source-tree imports,
checks packaged static UI assets, scans pinned dependencies, generates SBOM
evidence, emits GitHub artifact attestations, and uploads artifacts of record.
Publishing and container registry release remain intentionally out of scope and
are documented as pending maintainer publish intent.
