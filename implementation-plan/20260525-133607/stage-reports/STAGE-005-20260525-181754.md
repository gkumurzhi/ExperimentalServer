# STAGE-005 Closure Report
_Generated: 2026-05-25 18:17:54 MSK_

## Stage target
- Stage: STAGE-005 - Auth secret files and auth invariants
- Requested by: `$close-plan-stage STAGE-005 --from implementation-plan/20260525-133607/stages/STAGE-005-auth-secret-files-invariants.md --with-subagents`
- Active plan: `implementation-plan/20260525-133607`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied; stage has no dependencies

## Execution approach
1. Inspect current CLI, server, auth, tests, and auth documentation/examples for Basic Auth configuration flow.
2. Use a narrowly scoped subagent to map current auth paths and likely tests for this stage.
3. Implement `--auth-file` with fail-closed interaction against `--auth`, safe parsing, and non-secret errors.
4. Repair custom authenticator configuration so rate-limiter invariants are preserved.
5. Add targeted regression tests and update service/container docs to prefer secret files.
6. Run required targeted tests, lint/format/type checks where available, and static review for secret exposure.

## Acceptance criteria from plan
- [x] Service/container deployments can configure Basic Auth from a file path without passing the password in argv.
- [x] Auth-file parsing handles newline trimming and rejects malformed or empty files safely.
- [x] Error output and docs do not expose secret contents.
- [x] README custom-auth guidance preserves auth rate-limiter invariants.
- [x] Existing `--auth random` and explicit `--auth user:pass` behavior remains tested.

## Subagent log
| Agent | Scope | Result |
|---|---|---|
| explorer | Read-only auth CLI/server/tests/docs mapping for STAGE-005 | Completed; confirmed code paths and flagged direct `auth_file=""`, repeated trailing newline, README setter, and docs guidance risks. These were addressed in the final implementation. |

## Implementation summary
- Added `--auth-file FILE` CLI parsing and fail-closed validation when combined with `--auth`.
- Added server-side `auth_file` support that reads one UTF-8 `user:password` line, trims one trailing newline, rejects empty/malformed/multiline/invalid UTF-8/missing/unreadable files, and avoids echoing file contents in errors.
- Added `ExperimentalHTTPServer.set_authenticator()` so custom authenticators keep `_rate_limiter` synchronized.
- Updated README, SECURITY, and Docker Compose guidance to prefer `--auth-file` for services/containers/CI and to document `set_authenticator()`.
- Added targeted CLI and auth regression tests for auth-file success, conflict handling, redaction, parser defaults, invalid files, and rate-limiter invariants.

## Files changed
- `src/cli.py`
- `src/server.py`
- `tests/test_cli.py`
- `tests/test_security/test_auth.py`
- `README.md`
- `SECURITY.md`
- `examples/docker/docker-compose.yml`
- `implementation-plan/20260525-133607/stages/STAGE-005-auth-secret-files-invariants.md`
- `implementation-plan/20260525-133607/stage-status.md`
- `implementation-plan/20260525-133607/change-log.md`
- `implementation-plan/20260525-133607/stage-reports/STAGE-005-20260525-181754.md`

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
| Targeted tests | `python -m pytest tests/test_cli.py tests/test_security/test_auth.py tests/test_request_pipeline.py` | PASS | Final run: 158 passed. |
| Docs stale guard | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
| Compose config | `docker compose -f examples/docker/docker-compose.yml config` | PASS | Compose file renders successfully. |
| Lint | `ruff check src tests` | PASS | First run found one import-order issue in `tests/test_security/test_auth.py`; fixed with `ruff check --fix tests/test_security/test_auth.py`, then reran successfully. |
| Format | `ruff format --check src tests` | PASS | 74 files already formatted after the import-order fix. |
| Direct mypy availability | `mypy src` and `python -m mypy src` | FAIL | Local active Python did not have a working `mypy` module. |
| Typecheck recovery attempt | `uv run --extra dev mypy src` | FAIL | The project `.venv` had a local package-state conflict around `acme`; `dev` also does not include `mypy`. |
| Typecheck recovery attempt | `UV_PROJECT_ENVIRONMENT=/tmp/exphttp-stage005-venv uv run --extra dev mypy src` | FAIL | Throwaway env succeeded, but `dev` extra does not include `mypy`. |
| Typecheck | `UV_PROJECT_ENVIRONMENT=/tmp/exphttp-stage005-venv uv run --extra lint mypy src` | PASS | Success: no issues found in 34 source files. |
| Static secret/doc review | `rg -n -e "--auth" -e "authenticator =" -e "admin:" -e "mysecret" -e "replace-with-a-strong-secret" -e "auth-file" README.md SECURITY.md examples/docker/docker-compose.yml src tests` | PASS | Remaining inline credentials are local/interactive docs or tests; service/container examples use `--auth-file`. |
| Final targeted tests after lint fix | `python -m pytest tests/test_cli.py tests/test_security/test_auth.py tests/test_request_pipeline.py` | PASS | 158 passed. |
| Diff review | `git diff -- src/cli.py src/server.py tests/test_cli.py tests/test_security/test_auth.py README.md SECURITY.md examples/docker/docker-compose.yml implementation-plan/20260525-133607/stage-reports/STAGE-005-20260525-181754.md` | PASS | Changes match STAGE-005 scope. |
| Whitespace check | `git diff --check` | PASS | No whitespace errors. |

## Outcome
- Status: CLOSED
- Completed at: 2026-05-25 18:41:08 MSK
- Residual risks: none known for this stage. The first `uv run --extra dev` attempt exposed a local `.venv` package-state issue, but the required mypy verification passed in an isolated `/tmp` uv environment with the correct `lint` extra.
