# STAGE-008 Closure Report
_Generated: 2026-05-25 19:48:15 MSK_

## Stage target
- Stage: STAGE-008 - Docker and operational readiness
- Requested by: `$close-plan-stage STAGE-008 --from implementation-plan/20260525-133607/stages/STAGE-008-docker-ops-readiness.md --with-subagents`
- Active plan: `implementation-plan/20260525-133607`

## Baseline
- Branch: `main`
- Pre-existing changes: none
- Dependencies: satisfied (`STAGE-005`, `STAGE-006`, and `STAGE-007` are closed)

## Execution approach
1. Inspect the Dockerfile, Compose example, CI Docker smoke, CLI flags, and Docker/security documentation for STAGE-008 gaps.
2. Use a focused `explorer` subagent to independently map Docker/ops gaps in the selected stage paths.
3. Update the Compose example to use loopback default publication, secret-file auth examples, explicit health behavior, and operational resource guidance.
4. Extend CI Docker smoke so it validates Docker health status in default HTTP mode and covers a mode-aware TLS/auth health alternative.
5. Update README/SECURITY guidance for Docker exposure, health modes, ACME renewal/restart, and resource sizing.
6. Run Compose, Docker, docs, and static validation checks; update plan artifacts with the final status.

## Acceptance criteria from plan
- [x] Default Compose example does not publish plain HTTP on all host interfaces.
- [x] Compose auth examples use mounted secret files, not inline passwords.
- [x] Docker health behavior is explicit for default HTTP, auth/TLS, and ACME profiles.
- [x] CI Docker smoke validates default health status or records a clear mode-aware alternative.
- [x] Docs describe resource sizing from workers, body budget, and storage quotas.

## Subagents
- `explorer` (`019e6009-f140-75b2-8323-3a82df1fbb4f`): mapping Docker/Compose/CI/docs gaps for this stage.
- Result: completed read-only mapping of the scoped Docker/Compose/CI/docs paths. It confirmed the default all-interface Compose publish, missing concrete auth/TLS health behavior, missing CI default-health inspection, Docker-specific resource guidance gaps, and missing ACME restart/renewal guidance.

## Files changed
- `.github/workflows/ci.yml` - Docker smoke now waits for default container health, runs TLS/auth with a mounted auth file, and asserts TLS/auth smoke uses a disabled image healthcheck plus external HTTPS PING.
- `examples/docker/docker-compose.yml` - default HTTP publish is loopback-only; added `auth-tls` profile with mounted secret file and HTTPS/Auth healthcheck; added stop grace and commented resource envelope guidance; ACME keeps disabled healthcheck explicit.
- `examples/docker/secrets/.gitignore` - ignores local Compose secret files while keeping the directory scaffold.
- `README.md` - documents loopback Compose default, auth/TLS secret-file profile, health behavior by mode, ACME restart-before-expiry policy, and Docker resource sizing.
- `SECURITY.md` - adds Docker hardening, resource limit alignment, and ACME state/restart guidance.
- `implementation-plan/20260525-133607/stages/STAGE-008-docker-ops-readiness.md` - updated stage status and completion notes.
- `implementation-plan/20260525-133607/stage-status.md` - updated STAGE-008 row.
- `implementation-plan/20260525-133607/change-log.md` - appended closure entry.

## Verification log
| Check | Command | Result | Notes |
|---|---|---|---|
- Compose syntax/default publish | `docker compose -f examples/docker/docker-compose.yml config` | PASS | Rendered default service with `host_ip: 127.0.0.1` for port 8080. |
- Compose auth/TLS profile syntax | `docker compose -f examples/docker/docker-compose.yml --profile auth-tls config` | PASS | Rendered mounted `exphttp_auth` secret and HTTPS/Auth healthcheck. |
- Compose ACME profile syntax | `docker compose -f examples/docker/docker-compose.yml --profile acme config` | PASS | Rendered ACME service with `healthcheck.disable: true`. |
- CI Docker shell syntax | Extract `Docker smoke` run block from `.github/workflows/ci.yml` and run `bash -n` | PASS | Docker smoke script parses. |
- Docs stale guard | `python tools/check_stale_docs.py` | PASS | No stale documented contract references found. |
- Docker build | `docker build -t exphttp:ops-smoke .` | PASS | Image built successfully. |
- Docker default health smoke | `docker run -d --name exphttp-health-smoke -p 127.0.0.1::8080 exphttp:ops-smoke`; poll Docker health; PING via curl | PASS | Default container reached healthy and returned `pong`. |
- Docker TLS/auth smoke | `docker run -d --name exphttp-tls-auth-smoke --no-healthcheck ... --tls --auth-file /run/secrets/exphttp_auth`; HTTPS PING with Basic Auth | PASS | Found and fixed an initial non-root bind-mounted secret permission issue; rerun passed with a readable temporary fixture secret. |
- Compose auth/TLS runtime smoke | Create ignored `examples/docker/secrets/exphttp_auth`; `docker compose -f examples/docker/docker-compose.yml --profile auth-tls up -d --no-build exphttp-auth-tls`; poll health; HTTPS PING with Basic Auth | PASS | Confirmed local Compose file-backed secret must be readable by container UID 10001; docs now state this. |
- Diff whitespace | `git diff --check` | PASS | No whitespace errors. |

## Outcome
CLOSED. All STAGE-008 acceptance criteria were met, Docker was available for runtime validation, and the mode-aware health behavior is covered by Compose config, CI smoke, and local Docker/Compose runtime checks.
